export const GUIDE_BODIES = {
  'free-hosting-with-github-pages': `<h2>Introduction</h2>
<p>GitHub Pages is one of the most popular free hosting solutions for static websites, offering seamless integration with GitHub repositories and automatic deployments. Combined with a free is-cool-me subdomain, you can have a professional website live on the internet within minutes without spending a cent. This guide walks through every step of the process, from setting up your repository to configuring the custom subdomain with SSL.</p>
<h2>Prerequisites</h2>
<ul><li>A GitHub account with a repository containing your static site files</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Your site files ready (HTML, CSS, JS) or a static site generator configured</li></ul>
<h2>Step 1: Prepare Your Repository for GitHub Pages</h2>
<p>Navigate to your GitHub repository and go to "Settings &rarr; Pages". Under "Build and deployment", select "Deploy from a branch". Choose the branch you want to deploy (typically <code>main</code> or <code>gh-pages</code>) and set the root folder to <code>/</code> (root) or <code>/docs</code> depending on where your site files live. GitHub Pages will build and serve your site at <code>https://{username}.github.io/{repository}/</code>. If you are using a static site generator like Jekyll, Hugo, or Eleventy, commit the built output to your chosen branch or configure a GitHub Action to build on push.</p>
<h2>Step 2: Configure Your is-cool-me Subdomain</h2>
<p>Log into the is-cool-me dashboard at dash.is-pro.dev. If you have not registered your subdomain yet, enter your desired name and click register. Once registered, you will see the DNS management interface powered by Cloudflare. This is where you will add the DNS records that connect your subdomain to GitHub Pages. Take note of your GitHub Pages URL — it will be something like <code>username.github.io</code>.</p>
<h2>Step 3: Add a CNAME Record in the DNS Dashboard</h2>
<p>In the is-cool-me DNS dashboard, click "Add Record". Select CNAME as the record type. For the name field, enter your subdomain prefix (e.g., <code>myproject</code> if your subdomain is <code>myproject.is-pro.dev</code>). For the target field, enter your GitHub Pages URL, such as <code>username.github.io</code>. Make sure there is no trailing dot unless your provider requires it. Save the record. Cloudflare will propagate this change nearly instantly across its global network of 330+ data centers.</p>
<h2>Step 4: Set the Custom Domain in GitHub Pages Settings</h2>
<p>Go back to your GitHub repository settings, navigate to "Pages", and enter your full is-cool-me subdomain in the "Custom domain" field (e.g., <code>myproject.is-pro.dev</code>). Click "Save". GitHub will automatically create a CNAME file in your repository and attempt to provision a Let's Encrypt SSL certificate. This certificate may take up to 20 minutes to issue, during which your site may show a warning. If you see a message about "Enforcing HTTPS", check the box to force all traffic over HTTPS once the certificate is ready.</p>
<h2>Step 5: Verify DNS Resolution and SSL</h2>
<p>Wait a few minutes for DNS propagation (though Cloudflare propagates within seconds for most regions). Open a terminal and run <code>nslookup myproject.is-pro.dev</code> or use the DNS Checker tool at is-cool-me. The result should show GitHub's IP addresses. Then visit your subdomain in a browser. You should see your site loading over HTTPS with a valid certificate. If you see a 404, ensure that your GitHub Pages source branch is correctly configured and that the repository contains an <code>index.html</code> file at the root.</p>
<h2>Best Practices for GitHub Pages + Custom Domain</h2>
<ul><li>Enable "Enforce HTTPS" in GitHub Pages settings as soon as the certificate provisions — this prevents insecure connections and improves SEO rankings</li><li>Use a <code>.nojekyll</code> file in your repository root if you are not using Jekyll and want files or directories starting with an underscore to be served correctly</li><li>Keep your repository public (GitHub Pages is free only for public repositories) or upgrade to GitHub Pro for private repository hosting</li><li>Set a low TTL (60 seconds) on your DNS records during initial setup so changes propagate quickly; increase it to 300 or 3600 once everything is stable</li><li>Add a 404.html page to your repository for a custom error page when visitors mistype URLs</li></ul>
<h2>Conclusion</h2>
<p>GitHub Pages combined with an is-cool-me subdomain gives you a production-ready static site with automatic deployments, free SSL, and a professional URL — all at zero cost. This setup works perfectly for project documentation, personal blogs, portfolio sites, and landing pages. Once configured, every push to your repository automatically redeploys your site.</p>
<h2>FAQ</h2>
<p><strong>Does GitHub Pages support custom domains on the free tier?</strong></p>
<p>Yes, GitHub Pages supports custom domains on all tiers, including free. You just need a CNAME record pointing GitHub's servers.</p>
<p><strong>Why is my site showing a 404 after setting up the custom domain?</strong></p>
<p>This typically means the DNS has propagated but GitHub has not yet provisioned the SSL certificate, or the CNAME file in your repository does not match the domain. Check that the custom domain in your GitHub Pages settings exactly matches your subdomain.</p>
<p><strong>Can I use GitHub Pages for a single-page React app?</strong></p>
<p>Yes, but you need to configure client-side routing carefully. Set the <code>homepage</code> field in <code>package.json</code> to your subdomain URL, and use <code>HashRouter</code> instead of <code>BrowserRouter</code> to avoid 404 errors on page reload.</p>
<p><strong>How long does the SSL certificate take to provision?</strong></p>
<p>GitHub Pages uses Let's Encrypt and typically provisions certificates within 5-20 minutes. If it takes longer, ensure your DNS CNAME record resolves correctly and try removing and re-adding the custom domain in GitHub settings.</p>`,

  'deploy-react-app-to-vercel': `<h2>Introduction</h2>
<p>Vercel is a premier deployment platform for frontend applications, offering seamless Git integration, automatic SSL, and a global edge network. When you pair it with a free is-cool-me subdomain, you get a production-grade React app deployment with zero infrastructure management. This guide covers the complete workflow from a local React project to a live, HTTPS-enabled site on your custom subdomain.</p>
<h2>Prerequisites</h2>
<ul><li>A React application built with Create React App, Vite, or any React framework</li><li>A GitHub, GitLab, or Bitbucket account for repository hosting</li><li>A Vercel account (free tier, connect with your Git provider)</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li></ul>
<h2>Step 1: Push Your React App to Git</h2>
<p>Initialize a Git repository in your React project root if you have not already: <code>git init</code>. Add all files with <code>git add .</code> and commit with <code>git commit -m "Initial commit"</code>. Create a repository on GitHub, GitLab, or Bitbucket, then link your local repository: <code>git remote add origin https://github.com/username/repository.git</code>. Push your code with <code>git push -u origin main</code>. Before pushing, verify your app builds successfully by running <code>npm run build</code> or <code>yarn build</code> locally — this catches any compilation errors early.</p>
<h2>Step 2: Import Your Repository into Vercel</h2>
<p>Log into your Vercel dashboard at vercel.com. Click "Add New &rarr; Project". Select your Git provider and authorize Vercel to access your repositories. Find and select the repository containing your React app. Vercel automatically detects the framework (Create React App, Next.js, Vite, etc.) and pre-fills the build command and output directory. For a Create React App project, the build command is <code>npm run build</code> and the output directory is <code>build</code>. For Vite, the output directory is <code>dist</code>. Click "Deploy" and wait for the initial build to finish — typically 1-3 minutes. Vercel assigns a random <code>.vercel.app</code> domain where your app is immediately accessible.</p>
<h2>Step 3: Add Your is-cool-me Subdomain in Vercel</h2>
<p>In your Vercel project dashboard, navigate to "Settings &rarr; Domains". Enter your is-cool-me subdomain (e.g., <code>myapp.is-pro.dev</code>) and click "Add". Vercel will validate ownership by checking for a specific DNS record. It displays the required DNS target — usually a CNAME pointing to <code>cname.vercel-dns.com</code> or an A record pointing to Vercel's IP addresses. Copy this target value exactly as shown; you will need it in the next step.</p>
<h2>Step 4: Configure DNS in the is-cool-me Dashboard</h2>
<p>Open the is-cool-me DNS dashboard for your subdomain. Click "Add Record". Select CNAME as the record type. In the name field, enter your subdomain prefix (e.g., <code>myapp</code>). In the target field, paste the CNAME target from Vercel (e.g., <code>cname.vercel-dns.com</code>). Save the record. For the root domain (<code>is-pro.dev</code>), Vercel requires A records pointing to specific IPs — but since is-cool-me manages the root zone, you only need to configure your subdomain. Cloudflare proxies the traffic by default (orange cloud), which provides DDoS protection and SSL termination. If your Vercel deployment involves WebSockets or serverless functions that need direct connections, switch to DNS-only (gray cloud).</p>
<h2>Step 5: Configure Environment Variables and Production Settings</h2>
<p>In your Vercel project settings, go to "Environment Variables". Add any variables your React app needs in production, such as API endpoints or feature flags. You can set different values for Preview, Production, and Development environments. Vercel injects these at build time, so you must redeploy after adding or changing environment variables. If your app uses client-side routing (React Router, etc.), add a <code>vercel.json</code> file in your repository root with rewrites to handle client-side routes: <code>{"rewrites": [{"source": "/(.*)", "destination": "/index.html"}]}</code>.</p>
<h2>Step 6: Verify the Live Deployment</h2>
<p>Visit your is-cool-me subdomain in a browser. Your React app should load over HTTPS with a valid certificate issued by Let's Encrypt or Cloudflare. Test all routes, API calls, and interactive features. Open the browser's developer tools and check the Console and Network tabs for errors. Run Vercel's deployment checks by going to your project dashboard and selecting "Checks" — this validates DNS resolution, SSL, and performance. If you see a Vercel-branded 404 page, the CNAME record may point to the wrong target.</p>
<h2>Best Practices</h2>
<ul><li>Configure automatic deployments: Vercel deploys every push to the production branch. Set up preview deployments for staging branches to test before merging</li><li>Use Vercel's Analytics to monitor real-user performance metrics like LCP, CLS, and FID for your React app</li><li>Set Cache-Control headers via <code>vercel.json</code> or your hosting configuration to optimize asset delivery</li><li>Keep your <code>package.json</code> lockfile in version control for reproducible builds across deployments</li></ul>
<h2>Conclusion</h2>
<p>Vercel and is-cool-me form a powerful combination for deploying React applications. Vercel handles the build pipeline, global CDN, and automatic HTTPS, while your free subdomain provides a clean, branded URL. The entire setup takes under 15 minutes and costs nothing.</p>
<h2>FAQ</h2>
<p><strong>Does Vercel's free tier support custom domains?</strong></p>
<p>Yes, the free Hobby tier supports custom domains with automatic SSL. There are no additional charges for using your own domain.</p>
<p><strong>Why is my React app showing a blank page after deployment?</strong></p>
<p>This often happens when client-side routing is not configured. Add a <code>vercel.json</code> with rewrite rules, or check the browser console for JavaScript errors. Also verify that your <code>homepage</code> field in <code>package.json</code> does not interfere with asset paths.</p>
<p><strong>Can I use a custom domain with Vercel's free tier?</strong></p>
<p>Yes, Vercel's Hobby tier includes custom domain support with automatic SSL.</p>
<p><strong>How do I migrate from Vercel's .vercel.app domain to my custom subdomain?</strong></p>
<p>Simply add your subdomain in Vercel's Domains settings and configure the DNS CNAME record. Vercel will serve your app from both domains until you remove the default one.</p>`,

  'nextjs-hosting-on-cloudflare-pages': `<h2>Introduction</h2>
<p>Cloudflare Pages provides a fast, globally distributed hosting platform for JAMstack and server-side rendered applications. With built-in support for Next.js, you can deploy static sites, incremental static regeneration (ISR) pages, and server-side rendered routes without managing servers. Combined with an is-cool-me subdomain powered by Cloudflare's own DNS, you get the tightest possible integration between your hosting and DNS provider.</p>
<h2>Prerequisites</h2>
<ul><li>A Next.js application (version 12 or later recommended for full Cloudflare Pages compatibility)</li><li>A GitHub or GitLab account for repository hosting</li><li>A Cloudflare account (free tier)</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li></ul>
<h2>Step 1: Prepare Your Next.js App for Cloudflare Pages</h2>
<p>Cloudflare Pages supports Next.js through the <code>@cloudflare/next-on-pages</code> package, which adapts Next.js for Cloudflare's edge runtime. Install it: <code>npm install @cloudflare/next-on-pages</code> or <code>yarn add @cloudflare/next-on-pages</code>. Update your <code>next.config.js</code> to include the Cloudflare Pages adapter. Configure your build command to use <code>npx @cloudflare/next-on-pages</code> instead of the default <code>next build</code>. This generates an output compatible with Cloudflare's edge infrastructure, ensuring that server-side rendering, API routes, and middleware all work within Cloudflare's Worker environment. For static-only Next.js sites (SSG), you can skip the adapter and use the default build command.</p>
<h2>Step 2: Push to Git and Connect to Cloudflare Pages</h2>
<p>Push your Next.js project to a GitHub or GitLab repository. Log into the Cloudflare dashboard, navigate to "Workers & Pages", and click "Create &rarr; Pages &rarr; Connect to Git". Authorize Cloudflare to access your repositories and select your Next.js project. Cloudflare Pages will detect the framework and suggest a build command. For SSG-only projects, the default <code>npm run build</code> works. For projects using the adapter, set the build command to <code>npx @cloudflare/next-on-pages</code> and the output directory to <code>.vercel/output/static</code>. Click "Begin setup" and then "Deploy".</p>
<h2>Step 3: Configure Your is-cool-me Subdomain</h2>
<p>In the Cloudflare Pages project dashboard, go to "Custom domains". Click "Set up a custom domain" and enter your full is-cool-me subdomain (e.g., <code>mynextapp.is-pro.dev</code>). Since is-cool-me is already on Cloudflare DNS, Cloudflare Pages will detect this and automatically configure the DNS records. No manual DNS changes are needed in the is-cool-me dashboard — Cloudflare handles both the DNS and the hosting, creating the necessary CNAME record automatically. SSL is provisioned instantly through Cloudflare's edge certificates, which are free and auto-renewing.</p>
<h2>Step 4: Enable Edge Caching and Performance Features</h2>
<p>Cloudflare Pages automatically caches static assets at the edge across 330+ data centers worldwide. For dynamic routes, you can configure caching rules in the Cloudflare dashboard under "Caching &rarr; Configuration". Use Cache Rules or Page Rules to set cache durations for specific URL patterns. Your Next.js ISR pages benefit from Cloudflare's cache, serving stale content instantly while revalidating in the background. You can also enable Cloudflare Web Analytics (free) to track performance metrics like Time to First Byte (TTFB) and First Contentful Paint (FCP) across different geographic regions.</p>
<h2>Step 5: Configure Environment Variables and Build Settings</h2>
<p>In your Cloudflare Pages project, go to "Settings &rarr; Environment variables". Add variables for different environments (Production, Preview). Common Next.js environment variables include <code>NEXT_PUBLIC_API_URL</code> for your API endpoint and <code>NEXT_PUBLIC_SITE_URL</code> for the canonical site URL. Cloudflare Pages supports preview deployments for every branch and pull request, each with a unique URL. You can set different environment variable values for preview deployments to point to staging APIs or databases.</p>
<h2>Step 6: Deploy and Verify</h2>
<p>Trigger a deployment by pushing to your repository's production branch. Cloudflare Pages builds and deploys your Next.js app, typically within 1-5 minutes depending on project size. Visit your is-cool-me subdomain in a browser. Navigate between pages to verify that static pages load instantly, dynamic routes render correctly, and API routes respond as expected. Check the browser's Network tab to confirm responses are served from Cloudflare's edge (look for <code>cf-cache-status</code> headers). Test on multiple devices and network conditions to ensure consistent performance globally.</p>
<h2>Best Practices</h2>
<ul><li>Use ISR (Incremental Static Regeneration) for content that changes periodically — it combines the speed of static pages with fresh data</li><li>Set sensible cache durations: static assets can be cached for 30+ days, while API responses might need revalidation every few minutes</li><li>Monitor build times and optimize with the Next.js bundle analyzer if deployments take too long</li><li>Use Cloudflare's WAF rules to protect your Next.js API routes from common web attacks</li></ul>
<h2>Conclusion</h2>
<p>Cloudflare Pages offers one of the fastest hosting platforms for Next.js applications, with tight DNS integration when using an is-cool-me subdomain. The automatic DNS configuration, instant SSL, and global edge network make this setup ideal for production Next.js applications of any size.</p>
<h2>FAQ</h2>
<p><strong>Does Cloudflare Pages support Next.js middleware?</strong></p>
<p>Yes, when using the <code>@cloudflare/next-on-pages</code> adapter, your Next.js middleware runs at the edge in Cloudflare Workers for minimal latency.</p>
<p><strong>Can I use a custom domain with the free tier?</strong></p>
<p>Yes, Cloudflare Pages free tier includes custom domain support with unlimited bandwidth and 500 builds per month.</p>
<p><strong>Why is my Next.js API route returning 404?</strong></p>
<p>This usually means the next-on-pages adapter is not configured correctly. Verify your build command and output directory in your Cloudflare Pages project settings, and ensure the adapter package is installed.</p>`,

  'free-developer-portfolio-hosting': `<h2>Introduction</h2>
<p>Your developer portfolio is your digital business card — it is often the first thing potential employers or clients see when they search for you online. A professional portfolio does not need expensive hosting or a premium domain. With free tools like GitHub Pages, Vercel, or Netlify, and a no-cost is-cool-me subdomain, you can build and host a polished portfolio site that showcases your skills, projects, and experience without spending a penny on infrastructure.</p>
<h2>Prerequisites</h2>
<ul><li>A GitHub account for hosting (or Vercel/Netlify account)</li><li>Basic HTML/CSS knowledge (or use a template)</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Portfolio content: projects, skills, work experience, and contact information</li></ul>
<h2>Step 1: Choose Your Portfolio Platform</h2>
<p>There are several approaches to building a developer portfolio. Option one: hand-code a custom HTML/CSS site for full control and minimal overhead. Option two: use a static site generator like Hugo or Eleventy for templating and content management. Option three: use a pre-built template from HTML5 UP, Start Bootstrap, or similar sites. Option four: deploy a portfolio built with a framework like React or Vue. For most developers, a hand-coded or template-based static site hosted on GitHub Pages offers the best balance of simplicity and customization. Your portfolio should include at minimum: an "About" section, a project showcase with links to live demos and source code, a skills section, work experience timeline, and a contact form or links to your professional profiles.</p>
<h2>Step 2: Build or Customize Your Portfolio</h2>
<p>Create an <code>index.html</code> file as the entry point. Structure it with semantic HTML5 elements: <code>&lt;header&gt;</code> for navigation, <code>&lt;main&gt;</code> for content sections, and <code>&lt;footer&gt;</code> for contact links. Use CSS Grid or Flexbox for responsive layouts that work on mobile and desktop. Optimize images with tools like Squoosh or TinyPNG to keep page loads fast. Add meta tags for SEO and social sharing: include a descriptive title, meta description, and Open Graph tags with a profile image. Your portfolio should load in under 2 seconds — use Lighthouse in Chrome DevTools to audit performance and fix issues like render-blocking resources or oversized images.</p>
<h2>Step 3: Deploy to GitHub Pages</h2>
<p>Create a GitHub repository named <code>{username}.github.io</code> for a user site, or a regular name for a project site. Push your portfolio files to the <code>main</code> branch. In the repository settings, go to "Pages" and set the source to "Deploy from branch: main, /root". Your site is immediately live at <code>https://{username}.github.io</code>. If you chose Vercel instead, connect your repository and it auto-deploys with a <code>.vercel.app</code> domain. Verify the deployment loads correctly before adding your custom subdomain.</p>
<h2>Step 4: Connect Your is-cool-me Subdomain</h2>
<p>In the is-cool-me DNS dashboard, add a CNAME record pointing your portfolio subdomain (e.g., <code>portfolio.is-pro.dev</code> or <code>yourname.is-pro.dev</code>) to your GitHub Pages URL (<code>username.github.io</code>). Save the record. Next, go to your GitHub Pages settings and enter the custom domain: <code>portfolio.is-pro.dev</code>. GitHub will create a CNAME file and provision a Let's Encrypt certificate. For Vercel deployments, add the subdomain in Vercel's Domains settings and follow the DNS instructions provided. SSL is provisioned automatically by both platforms.</p>
<h2>Step 5: Add Customizations and Polish</h2>
<p>Once the basic portfolio is live, add finishing touches: configure a custom 404 page that matches your design, add a blog or writing section if you write technical articles, set up form handling for your contact form (Netlify Forms works well for static sites), add Google Analytics or a privacy-friendly alternative like Plausible to track visitors, and submit your site to Google Search Console for indexing. Consider adding a dark mode toggle using CSS custom properties and a small JavaScript snippet — it is a subtle touch that impresses technical recruiters.</p>
<h2>Best Practices</h2>
<ul><li>Keep your portfolio URL simple and memorable — <code>firstname-lastname.is-pro.dev</code> or just <code>yourname.is-pro.dev</code> work well</li><li>Showcase 3-5 best projects with clear descriptions, technologies used, and links to live demos and source code</li><li>Include measurable impact in project descriptions (e.g., "Reduced API response time by 40%" rather than "Improved performance")</li><li>Add a downloadable PDF resume link for recruiters who want a traditional format</li></ul>
<h2>Conclusion</h2>
<p>A professional developer portfolio on a custom subdomain is achievable with entirely free tools. GitHub Pages or Vercel handle hosting and SSL, while is-cool-me provides the branded URL. The total cost is zero, yet the result is a polished, professional online presence that helps you stand out in the job market.</p>
<h2>FAQ</h2>
<p><strong>Can I use my own domain instead of an is-cool-me subdomain?</strong></p>
<p>Yes, the same techniques work with paid domains. However, an is-cool-me subdomain is completely free and includes Cloudflare DNS management and SSL — saving you $10-15/year on domain registration.</p>
<p><strong>How do I add a blog to my portfolio?</strong></p>
<p>You can add a blog section by creating individual HTML pages for each post and linking them from a blog index. For a more scalable approach, use a static site generator like Eleventy or Hugo that supports blog-like collections.</p>
<p><strong>Does a custom subdomain affect my portfolio's SEO?</strong></p>
<p>Using a custom subdomain like <code>yourname.is-pro.dev</code> provides SEO benefits similar to a custom domain. Search engines treat subdomains as distinct entities, so your portfolio can rank independently while benefiting from the parent domain's trust.</p>`,

  'dns-records-explained': `<h2>Introduction</h2>
<p>DNS records are the building blocks of the internet's addressing system. Every time someone visits your subdomain, sends an email, or connects to your server, DNS records translate human-readable names into network-usable addresses. Understanding these record types is essential for anyone managing online services. This guide explains each DNS record type in practical terms, with examples of how and when to use them with your is-cool-me subdomain.</p>
<h2>Prerequisites</h2>
<ul><li>An is-cool-me subdomain to practice with (free at dash.is-pro.dev)</li><li>Basic understanding of how domain names and IP addresses work</li><li>Access to a DNS dashboard (the is-cool-me dashboard provides Cloudflare-powered DNS)</li></ul>
<h2>A and AAAA Records: Mapping Names to IP Addresses</h2>
<p>An A record maps a domain or subdomain to an IPv4 address, such as <code>192.0.2.1</code>. This is the most fundamental DNS record type — without it, your subdomain cannot point to a server. For example, if your VPS has an IP address of <code>203.0.113.5</code>, you create an A record for <code>myserver.is-pro.dev</code> pointing to that IP. AAAA records do the same but for IPv6 addresses, which are longer (e.g., <code>2001:db8::1</code>). Modern best practice is to include both A and AAAA records to support clients on either protocol. When setting up a web server, always verify that your A record resolves to the correct IP by using a DNS lookup tool.</p>
<h2>CNAME Records: Aliasing One Name to Another</h2>
<p>A CNAME record creates an alias from one domain name to another. For instance, <code>www.is-pro.dev</code> might be a CNAME pointing to <code>is-pro.dev</code>. CNAME records are essential for platforms like GitHub Pages, Vercel, and Netlify — you create a CNAME from your subdomain to the platform's provided domain. Important rules: a CNAME cannot coexist with other record types on the same name, and it cannot be used at the zone apex (the bare <code>is-pro.dev</code> domain). If you need the apex to alias to another domain, use an ALIAS or ANAME record if your provider supports it, or use CNAME flattening.</p>
<h2>MX Records: Routing Email Delivery</h2>
<p>MX (Mail Exchange) records specify which mail servers are responsible for receiving email on behalf of your subdomain. Each MX record has a priority value — lower numbers are tried first. For example, to receive email at <code>hello@yourname.is-pro.dev</code>, you configure MX records pointing to your email provider's servers. Cloudflare Email Routing uses MX records like <code>route1.mx.cloudflare.net</code> (priority 10) and <code>route2.mx.cloudflare.net</code> (priority 20). Always configure at least two MX records with different priorities for redundancy. Missing MX records mean email addressed to your domain will bounce or be undeliverable.</p>
<h2>TXT Records: Storing Arbitrary Text Data</h2>
<p>TXT records store text information associated with your subdomain. They are most commonly used for email authentication: SPF (Sender Policy Framework) records authorize which servers can send email from your domain, DKIM (DomainKeys Identified Mail) records provide cryptographic signatures, and DMARC (Domain-based Message Authentication, Reporting & Conformance) records specify how receivers should handle unauthenticated email. A typical SPF TXT record looks like: <code>v=spf1 include:_spf.mx.cloudflare.net ~all</code>. TXT records are also used for domain ownership verification — services like Google Workspace, AWS, or GitHub ask you to add a specific TXT record to prove you control the domain.</p>
<h2>SRV Records: Locating Specific Services</h2>
<p>SRV records generalize the concept of MX records to any service. They specify a target host and port for a specific protocol and service. The format is: <code>_service._protocol.name TTL class SRV priority weight port target</code>. For example, Minecraft game servers often use SRV records to point to a non-standard port: <code>_minecraft._tcp.mc.is-pro.dev SRV 0 5 25565 mc-server.example.com</code>. SRV records are also used by Discord for voice channels, Microsoft Teams for SIP services, and SIP-based phone systems. They are less common than A/CNAME records but critical for gaming and communication services.</p>
<h2>Other Record Types: NS, CAA, PTR, and SOA</h2>
<p>NS (Nameserver) records indicate which DNS servers are authoritative for a zone. For is-cool-me subdomains, the NS records point to Cloudflare's nameservers and should not be modified. CAA (Certification Authority Authorization) records specify which certificate authorities are allowed to issue SSL certificates for your domain. Adding a CAA record for <code>letsencrypt.org</code> and <code>cloudflare.com</code> provides an extra security layer against unauthorized certificate issuance. PTR records (reverse DNS) map IP addresses back to domain names — they are managed by your hosting provider, not in your DNS dashboard. SOA (Start of Authority) records contain administrative information about the zone and are automatically managed.</p>
<h2>Best Practices</h2>
<ul><li>Always use DNS-only (gray cloud) for MX records — proxied MX records can interfere with email delivery</li><li>Set TTL values based on how frequently you expect to change records: use 60 seconds during migration, 300-3600 seconds for stable configurations</li><li>Add CAA records to restrict SSL certificate issuance to authorized providers only</li><li>Use a DNS checker tool to verify each record type after making changes</li></ul>
<h2>Conclusion</h2>
<p>DNS records form the foundation of how your subdomain interacts with the internet. Each record type serves a specific purpose, and knowing when to use A vs CNAME vs MX vs SRV is essential for configuring everything from websites to game servers. The is-cool-me DNS dashboard, powered by Cloudflare, supports all standard record types and provides instant propagation across the global network.</p>
<h2>FAQ</h2>
<p><strong>What is the difference between proxied (orange cloud) and DNS-only (gray cloud)?</strong></p>
<p>Proxied routes traffic through Cloudflare's CDN, hiding your origin IP and providing DDoS protection. DNS-only passes traffic directly to your server. Use proxied for web servers, DNS-only for email and non-HTTP services.</p>
<p><strong>Can I have multiple records of the same type for the same subdomain?</strong></p>
<p>Yes, you can have multiple A records for load balancing (round-robin DNS), multiple MX records for redundancy, and multiple TXT records for different purposes (SPF, DKIM, DMARC).</p>
<p><strong>How long does it take for DNS changes to take effect?</strong></p>
<p>With Cloudflare DNS on is-cool-me, changes typically propagate within seconds globally. The TTL value of the record determines how long clients cache the old value before rechecking.</p>`,
  'ssl-certificate-setup-guide': `<h2>Introduction</h2>
<p>SSL certificates are no longer optional for websites — browsers actively warn users when visiting HTTP sites, and search engines penalize non-HTTPS pages in rankings. An SSL certificate encrypts traffic between your visitors and your server, protecting sensitive data and building trust. With an is-cool-me subdomain, you get free SSL through Cloudflare's edge certificates, which are automatically provisioned and renewed. This guide explains how SSL works and how to ensure every page on your subdomain is served securely.</p>
<h2>Prerequisites</h2>
<ul><li>An is-cool-me subdomain with DNS managed through Cloudflare</li><li>A website or service hosted on your subdomain</li><li>Access to your origin server to configure redirect rules (optional but recommended)</li></ul>
<h2>How SSL Certificates Work</h2>
<p>SSL (or more accurately, TLS) uses public-key cryptography to establish an encrypted connection between a client (browser) and a server. When a visitor accesses your subdomain over HTTPS, the server presents its SSL certificate, which contains the domain name, the certificate authority (CA) that issued it, and the public key. The browser verifies the certificate against trusted CA roots. If valid, a symmetric session key is exchanged and all subsequent data is encrypted. Cloudflare's Universal SSL certificates are issued by Google Trust Services or Let's Encrypt and cover your subdomain automatically as long as DNS is proxied through Cloudflare (orange cloud enabled).</p>
<h2>Step 1: Enable Cloudflare Proxy on Your DNS Records</h2>
<p>In the is-cool-me DNS dashboard, ensure your subdomain's DNS record has the proxy status set to "Proxied" (orange cloud). This routes traffic through Cloudflare's network, where SSL termination occurs. Cloudflare's edge servers present the SSL certificate to visitors, then forward the request to your origin server either over HTTP or HTTPS (depending on your origin configuration). The proxy must be enabled for Cloudflare's Universal SSL to work — DNS-only (gray cloud) records will not receive an edge certificate and your visitors would connect directly to your origin server without Cloudflare's SSL protection.</p>
<h2>Step 2: Configure SSL/TLS Encryption Mode</h2>
<p>In your Cloudflare dashboard, navigate to "SSL/TLS &rarr; Overview". You have four encryption mode options: Off (not recommended), Flexible (encrypts traffic between visitor and Cloudflare only), Full (encrypts traffic end-to-end but allows self-signed origin certificates), and Full (strict) (encrypts end-to-end with a valid CA-signed origin certificate). For most setups with is-cool-me, Flexible mode is the easiest — Cloudflare terminates SSL at the edge and connects to your origin over HTTP. For maximum security, configure Full (strict) mode by installing a valid certificate on your origin server, such as one from Let's Encrypt.</p>
<h2>Step 3: Force HTTPS with Redirect Rules</h2>
<p>Even with SSL enabled, visitors might still access your subdomain over HTTP if they type the URL without HTTPS or click old links. In Cloudflare, go to "SSL/TLS &rarr; Edge Certificates" and enable "Always Use HTTPS". This redirects all HTTP requests to HTTPS automatically. You can also add a Page Rule for your subdomain: <code>yourname.is-pro.dev/*</code> with the setting "Always Use HTTPS" enabled. For additional security, enable "Automatic HTTPS Rewrites" which converts HTTP URLs in your page's HTML to HTTPS, preventing mixed content warnings.</p>
<h2>Step 4: Configure HSTS (HTTP Strict Transport Security)</h2>
<p>HSTS tells browsers to always connect to your subdomain over HTTPS, even if the user types HTTP in the address bar. This prevents downgrade attacks and SSL stripping. In Cloudflare, go to "SSL/TLS &rarr; Edge Certificates" and enable "HTTP Strict Transport Security (HSTS)". Start with a <code>max-age</code> of 3600 seconds (1 hour) for testing, then increase to 31536000 seconds (1 year) after confirming everything works. Include the <code>includeSubDomains</code> directive if you want all subdomains covered. Enable "Preload" to submit your domain to browser preload lists, giving you protection from the very first visit.</p>
<h2>Step 5: Verify SSL Configuration</h2>
<p>Use the SSL Checker tool on is-cool-me to verify your SSL certificate status. It will confirm the certificate issuer, expiration date, and encryption strength. Visit your subdomain in Chrome or Firefox and click the padlock icon in the address bar to view certificate details. Run an SSL Labs test (ssllabs.com) for a comprehensive security assessment of your configuration. Check for common issues like mixed content warnings (HTTP resources loaded on HTTPS pages), expired certificates, and weak cipher suites. A properly configured SSL setup earns an A+ rating on SSL Labs.</p>
<h2>Best Practices</h2>
<ul><li>Renew certificates before they expire — Cloudflare does this automatically for edge certificates, but origin certificates need manual renewal or Certbot automation</li><li>Use Full (strict) encryption mode when you control the origin server and can install a valid origin certificate</li><li>Monitor SSL expiration dates using the SSL Checker tool or set up calendar reminders</li><li>Test your site's SSL configuration after any DNS change, as misconfigured records can break certificate validation</li></ul>
<h2>Conclusion</h2>
<p>SSL configuration for an is-cool-me subdomain is straightforward thanks to Cloudflare's automatic certificate management. By enabling proxy mode, configuring the right encryption level, and enforcing HTTPS with HSTS, you ensure every visitor connects securely. This protects your users, improves your search engine ranking, and is a prerequisite for many web platform features like service workers and geolocation APIs.</p>
<h2>FAQ</h2>
<p><strong>Do I need to install anything on my server for Cloudflare SSL to work?</strong></p>
<p>In Flexible mode, no — Cloudflare handles SSL at the edge. For Full (strict) mode, you need a valid certificate on your origin server.</p>
<p><strong>Is the SSL certificate really free and auto-renewing?</strong></p>
<p>Yes, Cloudflare provides Universal SSL certificates for all proxied domains at no cost. They renew automatically with no action needed from you.</p>
<p><strong>Why does my subdomain show a certificate warning?</strong></p>
<p>This typically happens if your DNS record is set to DNS-only (gray cloud) rather than proxied (orange cloud). Check your DNS settings and ensure proxy is enabled.</p>`,

  'netlify-custom-domain-setup': `<h2>Introduction</h2>
<p>Netlify is a leading platform for deploying static sites and frontend applications, offering features like form handling, serverless functions, deploy previews, and split testing. Pairing Netlify with an is-cool-me subdomain gives you a professional hosting setup with global CDN, automatic HTTPS via Let's Encrypt, and continuous deployment from Git — all on the free tier. This guide walks through connecting your is-cool-me subdomain to Netlify step by step.</p>
<h2>Prerequisites</h2>
<ul><li>A Netlify account (free tier) with a deployed site</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Access to the is-cool-me DNS management interface (Cloudflare-powered)</li><li>Your site's Netlify-assigned URL (e.g., <code>mysite.netlify.app</code>)</li></ul>
<h2>Step 1: Deploy Your Site to Netlify</h2>
<p>If you have not deployed to Netlify yet, log into your Netlify dashboard and click "Add new site &rarr; Import an existing project". Connect your Git provider (GitHub, GitLab, or Bitbucket) and select the repository for your site. Netlify auto-detects the framework and pre-fills build settings. Review and click "Deploy site". Netlify assigns a random subdomain like <code>random-words-123456.netlify.app</code>. Your site is immediately live at this URL. Verify everything works before proceeding to add your custom subdomain.</p>
<h2>Step 2: Add Your is-cool-me Subdomain in Netlify</h2>
<p>In your Netlify site dashboard, go to "Site settings &rarr; Domain management &rarr; Custom domains". Click "Add custom domain". Enter your full is-cool-me subdomain (e.g., <code>mysite.is-pro.dev</code>). Netlify will prompt you to check DNS configuration. It provides the required DNS target — typically a CNAME record pointing to <code>mysite.netlify.app</code> or an A record pointing to Netlify's load balancer IPs. Note: if you are adding the bare domain (e.g., <code>is-pro.dev</code> itself), Netlify expects A records. For subdomains, a CNAME record suffices.</p>
<h2>Step 3: Configure DNS in the is-cool-me Dashboard</h2>
<p>Open the is-cool-me DNS dashboard. Click "Add Record". For a subdomain setup, select CNAME as the record type. Enter your subdomain prefix as the name (e.g., <code>mysite</code>) and your Netlify site URL (<code>mysite.netlify.app</code>) as the target. Important: Netlify recommends setting the proxy status to DNS-only (gray cloud) rather than proxied (orange cloud). This is because Netlify handles its own SSL via Let's Encrypt and needs direct traffic for certificate validation. Save the record and verify it appears in your DNS list.</p>
<h2>Step 4: Configure Netlify SSL / HTTPS</h2>
<p>Back in Netlify, under "Domain management &rarr; Custom domains", wait for Netlify to detect the DNS changes. Netlify automatically provisions a Let's Encrypt SSL certificate for your custom domain once it confirms DNS resolution. This process can take 1-15 minutes. Do not check "External DNS" if you are managing DNS through is-cool-me. Once the certificate is active, you will see a "Netlify Certificate" status with a green checkmark. Enable "Force HTTPS" to redirect all HTTP traffic to HTTPS.</p>
<h2>Step 5: Handle the WWW Subdomain Variant</h2>
<p>Many visitors still type <code>www.mysite.is-pro.dev</code> out of habit. In Netlify, add <code>www.mysite.is-pro.dev</code> as another custom domain. Then add a CNAME record in the is-cool-me dashboard: name it <code>www.mysite</code>, target it to <code>mysite.netlify.app</code>. Netlify handles the redirect from www to the non-www version (or vice versa, based on your configured primary domain). Set this up under "Domain management &rarr; Custom domains &rarr; Options &rarr; Set preferred domain".</p>
<h2>Step 6: Verify Full Setup</h2>
<p>Visit your is-cool-me subdomain in a browser. Confirm the site loads over HTTPS with a valid Let's Encrypt certificate. Check that the <code>www</code> variant redirects correctly to your primary domain. Test Netlify-specific features like deploy previews and form handling. Use the DNS Checker tool to confirm your CNAME record resolves to Netlify's servers. If you see a Netlify-branded page saying "This page is not found", your DNS record may not point to the correct target or the certificate has not yet provisioned.</p>
<h2>Best Practices</h2>
<ul><li>Use Netlify's deploy previews for staging — each branch or PR gets a unique URL to test before merging to production</li><li>Configure form handling in Netlify by adding <code>netlify</code> attribute to your HTML forms — submissions are collected in the Netlify dashboard</li><li>Set up branch-based subdomains (e.g., <code>staging.mysite.is-pro.dev</code>) by adding additional CNAME records</li></ul>
<h2>Conclusion</h2>
<p>Netlify combined with an is-cool-me subdomain provides a complete, free hosting solution for static sites and frontend applications. Netlify handles builds, deployments, and SSL, while your is-cool-me subdomain gives you a clean, professional URL backed by Cloudflare DNS.</p>
<h2>FAQ</h2>
<p><strong>Why does Netlify recommend DNS-only (gray cloud) instead of proxied?</strong></p>
<p>Netlify manages its own SSL certificates through Let's Encrypt and needs to verify domain ownership by connecting directly to your origin. Proxied mode (orange cloud) can interfere with this verification process.</p>
<p><strong>Can I use Netlify with a proxied (orange cloud) DNS record?</strong></p>
<p>Technically yes, but Netlify cannot provision its own SSL certificate in that case. You would rely on Cloudflare's edge certificate, which works but limits some Netlify features like custom 404 pages.</p>
<p><strong>How do I set up a redirect from my old domain to the new is-cool-me subdomain?</strong></p>
<p>Netlify supports redirects via a <code>_redirects</code> file or <code>netlify.toml</code>. Add the old domain as a custom domain and configure a redirect rule to your primary subdomain.</p>`,

  'subdomain-registry-guide': `<h2>Introduction</h2>
<p>Registering a subdomain on is-cool-me is the first step toward having a free, professionally managed web presence. The platform provides Cloudflare-powered DNS management, free SSL certificates, and a straightforward dashboard for handling all your domain records. This guide explains the complete lifecycle of an is-cool-me subdomain — from choosing the right name and registering it, to configuring DNS records, managing multiple projects, and keeping your subdomain active.</p>
<h2>Prerequisites</h2>
<ul><li>No previous account required — registration is open to everyone</li><li>A web browser to access dash.is-pro.dev</li><li>A clear idea of what you want to host on your subdomain</li></ul>
<h2>Step 1: Choosing a Subdomain Name</h2>
<p>Your subdomain name is your identity on the is-cool-me platform. Choose a name that reflects your project or personal brand. For a portfolio site, use your name or initials (e.g., <code>alex.is-pro.dev</code>). For a project, use the project name (e.g., <code>myapp.is-pro.dev</code>). Keep it short, memorable, and easy to type over the phone. Avoid hyphens, numbers, and ambiguous characters like zero vs the letter O. Check that the name is not already registered by searching on the dashboard. If your first choice is taken, try variations like adding your industry or location.</p>
<h2>Step 2: Registering Your Subdomain</h2>
<p>Navigate to dash.is-pro.dev and enter your desired subdomain name in the registration field. Click "Check Availability". If the name is available, click "Register". The system will provision your subdomain on Cloudflare's DNS infrastructure, creating the necessary zone records. This process takes less than 30 seconds. You will see your new subdomain listed in the dashboard with a default configuration. By default, your subdomain has Cloudflare DNS enabled with proxied (orange cloud) mode, providing DDoS protection and SSL termination from the moment of registration.</p>
<h2>Step 3: Understanding the DNS Dashboard</h2>
<p>The is-cool-me dashboard presents your DNS records in a table format. Each record shows: type (A, CNAME, MX, TXT, etc.), name (your subdomain prefix), value (the target), TTL (time to live — how long DNS responses are cached), and proxy status (orange cloud for proxied, gray cloud for DNS-only). The dashboard supports all standard record types needed for web hosting, email, and other services. You can add, edit, or delete records at any time. Changes propagate through Cloudflare's global network within seconds.</p>
<h2>Step 4: Adding DNS Records for Your Project</h2>
<p>Once your subdomain is registered, you need DNS records to connect it to your hosting or service. The type of records depends on what you are hosting: for GitHub Pages, add a CNAME record pointing to <code>username.github.io</code>. For a VPS, add an A record with your server's IP address. For email, add MX records pointing to your email provider. Always verify your records using a DNS checker tool after adding them. Common mistakes include missing trailing dots, wrong record types, and proxy settings that conflict with the service's requirements. For most web hosting platforms, a CNAME record with DNS-only (gray cloud) mode works best.</p>
<h2>Step 5: Managing Multiple Subdomains</h2>
<p>You can register multiple subdomains under your is-cool-me account, each pointing to different projects or services. The dashboard lists all your subdomains on a single page, making it easy to manage them. Use descriptive names for each subdomain to keep them organized: <code>blog.is-pro.dev</code>, <code>api.is-pro.dev</code>, <code>portfolio.is-pro.dev</code>, etc. Each subdomain is independent — changing DNS records for one does not affect others. This isolation makes it safe to experiment with new configurations on separate subdomains while keeping production services stable.</p>
<h2>Step 6: Keeping Your Subdomain Active</h2>
<p>is-cool-me subdomains are free but require periodic activity to remain active. The dashboard shows the status of each subdomain, including any inactivity warnings. To keep your subdomain active, ensure it has valid DNS records configured and is serving content. If you stop using a subdomain, you can release it from the dashboard so someone else can register it. Set a reminder to check your subdomain status every few months, especially if you registered it for a project that is temporarily on hold.</p>
<h2>Best Practices</h2>
<ul><li>Register multiple subdomains for different projects — they are free and isolated from each other</li><li>Use a naming convention that scales: <code>project-type.yourname.is-pro.dev</code> or just <code>projectname.is-pro.dev</code></li><li>Monitor your subdomains with the is-cool-me status dashboard and set up uptime monitoring for critical services</li><li>Remove DNS records for projects you have decommissioned to keep your dashboard clean</li></ul>
<h2>Conclusion</h2>
<p>The is-cool-me subdomain registry provides a fast, free way to establish a web presence with enterprise-grade DNS infrastructure. With Cloudflare powering the backend, your subdomain benefits from global CDN, DDoS protection, and free SSL from the moment of registration. Whether you need one subdomain for a portfolio or a dozen for various projects, the dashboard makes management simple.</p>
<h2>FAQ</h2>
<p><strong>How many subdomains can I register?</strong></p>
<p>There is no hard limit, but each subdomain should be used for an active project. Register only what you need to keep the platform sustainable for everyone.</p>
<p><strong>Can I transfer my subdomain to another user?</strong></p>
<p>Subdomain transfers are handled through the dashboard. Both parties need to confirm the transfer, and the receiving user must have an active account.</p>
<p><strong>What happens if I stop using my subdomain?</strong></p>
<p>After a period of inactivity, the subdomain may be released back to the pool for others to register. Check the platform's terms for the exact inactivity period.</p>`,

  'api-subdomain-with-aws-lambda': `<h2>Introduction</h2>
<p>AWS Lambda enables you to run serverless functions without provisioning or managing servers. Combined with API Gateway, you can build RESTful or HTTP APIs that scale automatically from zero to thousands of requests per second. By exposing your API through an is-cool-me subdomain, you get a clean, branded endpoint with Cloudflare's DNS management and SSL — all while keeping your infrastructure costs near zero for low-traffic APIs.</p>
<h2>Prerequisites</h2>
<ul><li>An AWS account with Lambda and API Gateway access</li><li>The AWS CLI installed and configured locally (optional but helpful)</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Basic knowledge of REST APIs and HTTP methods</li></ul>
<h2>Step 1: Create Your Lambda Function</h2>
<p>Log into the AWS Management Console and navigate to Lambda. Click "Create function" and choose "Author from scratch". Give your function a name (e.g., <code>my-api-handler</code>). Select a runtime — Node.js 18 or Python 3.11 are good choices. For permissions, create a basic execution role with Lambda basic execution permissions. Write your function code that handles API requests. A simple example returns a JSON response: <code>exports.handler = async (event) => { return { statusCode: 200, body: JSON.stringify({ message: 'Hello from Lambda' }) } }</code>. Click "Deploy" to save your function. Test it with a sample event to confirm it returns the expected response.</p>
<h2>Step 2: Set Up API Gateway</h2>
<p>Navigate to API Gateway in the AWS console. Click "Create API" and choose "REST API" or "HTTP API". HTTP API is simpler and cheaper for most use cases. Select "Build" and give your API a name (e.g., <code>my-api</code>). Create a route — for a basic API, add a <code>GET /</code> route and a <code>GET /{proxy+}</code> catch-all route. Attach your Lambda function as the integration target. Deploy your API to a stage (e.g., <code>prod</code>). API Gateway assigns an invoke URL like <code>https://abcdef123.execute-api.us-east-1.amazonaws.com</code>. Test this URL in your browser or with curl to verify your Lambda responds correctly.</p>
<h2>Step 3: Configure Custom Domain in API Gateway</h2>
<p>In API Gateway, go to "Custom domain names" in the left navigation. Click "Create". Enter your is-cool-me subdomain (e.g., <code>api.is-pro.dev</code>). For the endpoint configuration, choose "Regional" (not Edge-optimized, since Cloudflare will handle CDN). Create a TLS certificate using AWS Certificate Manager (ACM) in the same region as your API Gateway. ACM requires domain verification — it will give you a CNAME record to add to your DNS. You need to request the certificate for <code>api.is-pro.dev</code> and optionally a wildcard <code>*.api.is-pro.dev</code>. Once the certificate is issued (verification usually takes a few minutes), select it in the API Gateway custom domain settings and map your API stage to a path (e.g., map <code>GET /</code> to <code>$default</code>).</p>
<h2>Step 4: Configure DNS in the is-cool-me Dashboard</h2>
<p>Open the is-cool-me DNS dashboard. You need to add two DNS records. First, add the CNAME record that ACM provided during certificate verification (this verifies domain ownership for AWS). Second, add a CNAME record pointing your API subdomain to the API Gateway regional domain name, which looks like <code>d-abc1234.execute-api.us-east-1.amazonaws.com</code>. Important: set the proxy status to DNS-only (gray cloud) for the API Gateway CNAME record. Proxied mode (orange cloud) can interfere with API Gateway's certificate validation and may cause issues with non-HTTP traffic patterns used by API Gateway.</p>
<h2>Step 5: Configure CORS for Your API</h2>
<p>If your API is called from a frontend application on a different domain, you need to configure CORS (Cross-Origin Resource Sharing). In API Gateway, you can enable CORS at the API level or for individual routes. Add the <code>Access-Control-Allow-Origin</code> header with your frontend domain (or <code>*</code> for development). Also add <code>Access-Control-Allow-Methods</code> and <code>Access-Control-Allow-Headers</code>. If your Lambda returns these headers directly, ensure they are included in the response. Test CORS by making an HTTP request from your frontend and checking for CORS errors in the browser console. Use the HTTP Headers Checker tool on is-cool-me to verify your API's response headers.</p>
<h2>Step 6: Monitor and Optimize</h2>
<p>Set up CloudWatch monitoring for your Lambda function to track invocation counts, error rates, and duration. Configure basic alerts for 5xx errors exceeding a threshold. For cost management, monitor the AWS Free Tier limits: 1 million Lambda requests and 1 million API Gateway requests per month. Consider using Cloudflare's caching for GET endpoints that return static or slowly-changing data. Add rate limiting via API Gateway's usage plans if your API is publicly accessible. Use the is-cool-me uptime monitoring tools to track your API availability.</p>
<h2>Best Practices</h2>
<ul><li>Use environment variables in Lambda for configuration like database connection strings and API keys</li><li>Implement proper error handling with consistent JSON error responses</li><li>Enable AWS X-Ray tracing for debugging performance bottlenecks</li><li>Set API Gateway throttling limits to protect against unexpected traffic spikes</li></ul>
<h2>Conclusion</h2>
<p>AWS Lambda, API Gateway, and an is-cool-me subdomain form a powerful serverless stack for building and exposing APIs. The Lambda function handles business logic, API Gateway manages HTTP routing and scaling, and your subdomain provides a clean, branded endpoint with professional DNS management.</p>
<h2>FAQ</h2>
<p><strong>Does this setup work with the AWS Free Tier?</strong></p>
<p>Yes, AWS Free Tier includes 1 million Lambda requests and 1 million API Gateway requests per month for the first 12 months. After that, costs remain very low for small to moderate traffic.</p>
<p><strong>Can I use this with other cloud providers like Google Cloud Functions or Azure Functions?</strong></p>
<p>Yes, the DNS configuration approach is similar for any cloud provider. Instead of API Gateway, you would use Cloud Run, Cloud Functions, or Azure API Management with the same CNAME-based custom domain setup.</p>
<p><strong>Why does my API return a 403 error after setting up the custom domain?</strong></p>
<p>This usually indicates API Gateway's resource policy or AWS WAF is blocking the request. Check your API's resource policy to ensure it allows requests from the custom domain, and verify that the API Gateway stage is properly mapped to the custom domain path.</p>`,

  'cloudflare-proxy-vs-dns-only': `<h2>Introduction</h2>
<p>When managing DNS records in Cloudflare (which powers is-cool-me), every record has a toggle between "Proxied" (orange cloud) and "DNS-only" (gray cloud). This small setting has significant implications for security, performance, and compatibility. Understanding when to use each mode is essential for configuring your subdomains correctly. This guide explains the technical differences and provides clear guidance for every common scenario.</p>
<h2>Prerequisites</h2>
<ul><li>An is-cool-me subdomain with Cloudflare DNS enabled</li><li>A basic understanding of how HTTP and DNS work</li><li>Access to the is-cool-me DNS dashboard to view proxy toggles</li></ul>
<h2>What Happens in Proxied Mode (Orange Cloud)</h2>
<p>When a DNS record is set to Proxied, traffic to that subdomain is routed through Cloudflare's global network before reaching your origin server. Cloudflare terminates the TCP connection at the edge, inspects the traffic for threats (DDoS, SQL injection, XSS), caches static content, and optionally optimizes images and scripts. The visitor sees Cloudflare's IP address in their DNS resolution, not your origin server's IP — effectively hiding your server from direct internet exposure. This is why proxied mode is sometimes called "orange cloud" mode. Cloudflare also terminates SSL at the edge, presenting your visitors with a valid certificate even if your origin server uses a self-signed certificate. Proxied mode supports Cloudflare features like Page Rules, Workers, Argo Smart Routing, and WAF (Web Application Firewall).</p>
<h2>What Happens in DNS-Only Mode (Gray Cloud)</h2>
<p>In DNS-only mode, Cloudflare simply responds to DNS queries with the record's value — typically your origin server's IP address or a CNAME target. Traffic flows directly from the visitor to your server without passing through Cloudflare's network. Cloudflare does not cache content, inspect traffic, or terminate SSL. The proxy toggles on the DNS record are disabled, and Cloudflare functions only as an authoritative nameserver. Your origin IP is visible to anyone who looks up your DNS records. DNS-only mode is necessary for services that use protocols other than HTTP/HTTPS (such as SMTP for email, SSH for remote access, or WebSockets without HTTP wrapping), because Cloudflare's proxy only handles HTTP and WebSocket traffic on ports 80 and 443.</p>
<h2>When to Use Proxied Mode</h2>
<p>Use proxied mode for any web server serving HTTP or HTTPS content: portfolio sites, blogs, landing pages, web applications, API endpoints served over HTTPS, and static sites hosted on VPS or shared hosting. Proxied mode is also recommended for WordPress sites, custom web applications, and any service where hiding the origin IP is important for security. If you are not sure which mode to use, start with proxied — it provides the most benefits. The only significant downside is that Cloudflare must see the traffic to serve it, so the origin server must accept connections from Cloudflare's IP ranges.</p>
<h2>When to Use DNS-Only Mode</h2>
<p>DNS-only mode is required for services that do not use HTTP: email servers (SMTP on port 25, IMAP on port 143, POP3 on port 110), SSH access (port 22), FTP (ports 20-21), game servers (Minecraft on port 25565, etc.), and any service using non-standard ports. DNS-only mode is also needed for platforms that manage their own SSL and require direct traffic. Netlify, for example, recommends DNS-only mode because it needs direct HTTPS traffic to provision and verify Let's Encrypt certificates. Some API Gateway configurations also require DNS-only mode for certificate validation. If your service provides its own CDN, caching, or SSL termination, DNS-only mode prevents conflicts with Cloudflare's proxy.</p>
<h2>How Caching Differs Between the Two Modes</h2>
<p>In proxied mode, Cloudflare caches static content (HTML, CSS, JS, images) according to cache rules you configure. This dramatically reduces load on your origin server and improves page load times for visitors worldwide. You can set cache duration, purge cached content, and use cache keys to customize behavior. In DNS-only mode, no caching occurs at Cloudflare's edge — every request hits your origin server directly. If you need a CDN in DNS-only mode, you must use a separate CDN provider or configure caching at your application level. For high-traffic sites, proxied mode can reduce origin bandwidth by 60-80%.</p>
<h2>Security Implications</h2>
<p>Proxied mode provides DDoS protection, IP obfuscation, SSL termination, WAF rules, bot management, and rate limiting — all built into Cloudflare's free tier. DNS-only mode exposes your origin IP and provides none of these protections. If you must use DNS-only mode for a specific service (like email), consider securing that service independently: use strong authentication, restrict access by IP, enable firewall rules, and monitor logs for suspicious activity. For hybrid setups where some records are proxied and others are DNS-only, the proxied records still benefit from Cloudflare's security features even on the same subdomain.</p>
<h2>Best Practices</h2>
<ul><li>Default to proxied (orange cloud) for all HTTP/HTTPS services and switch to DNS-only only when compatibility requires it</li><li>For email-related records (MX, TXT), always use DNS-only — proxied email records break delivery</li><li>Test both modes to see which provides better performance for your specific use case</li><li>If you need SSL proxied but have an API on a non-standard port, consider using Cloudflare Workers or a reverse proxy to bridge the traffic</li></ul>
<h2>Conclusion</h2>
<p>The proxy toggle in your is-cool-me DNS dashboard determines whether Cloudflare actively protects and accelerates your traffic or simply resolves DNS. Proxied mode offers security, performance, and SSL benefits for web services. DNS-only mode is necessary for non-HTTP services and platforms with their own SSL management. Understanding this distinction is fundamental to correctly configuring your subdomains.</p>
<h2>FAQ</h2>
<p><strong>Can I change a record from proxied to DNS-only without downtime?</strong></p>
<p>There may be a brief interruption as DNS caches update. Lower the TTL to 60 seconds before switching, wait for propagation, then change the proxy setting.</p>
<p><strong>Does DNS-only mode affect SSL?</strong></p>
<p>Yes. In DNS-only mode, Cloudflare does not provide SSL termination. Your origin server must have its own valid SSL certificate for HTTPS to work.</p>
<p><strong>Can I use proxied mode for a subdomain that hosts a game server?</strong></p>
 <p>Only if the game client communicates over HTTP or WebSocket on ports 80/443. Most game servers use custom protocols on arbitrary ports and require DNS-only mode.</p>`,
  'anime-website-hosting-guide': `<h2>Introduction</h2>
<p>Anime fan sites and wikis are passion projects that deserve reliable hosting without monthly costs eating into your budget. Whether you are building a character database, episode guide, fan art gallery, or full wiki, you can host it on a free is-cool-me subdomain using tools like MediaWiki, Hugo, or Jekyll. This guide covers the complete setup from choosing the right tool to deploying with free hosting platforms and configuring your custom subdomain.</p>
<h2>Prerequisites</h2>
<ul><li>A GitHub account for hosting static sites on GitHub Pages</li><li>Basic knowledge of Markdown or HTML for content creation</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Content ready: character descriptions, episode summaries, images (optimized for web)</li></ul>
<h2>Step 1: Choose Your Site Builder</h2>
<p>Select a tool based on the type of site you want to create. For a full wiki with search, categories, and user contributions, MediaWiki (the software behind Wikipedia) is the gold standard but requires PHP hosting. For a static information site, Hugo or Jekyll are excellent choices — they generate fast HTML pages from Markdown content and are free to host on GitHub Pages or Cloudflare Pages. For a simple gallery site, a hand-coded HTML page or a template-based site works well. Hugo is particularly well-suited for anime fan sites because of its taxonomy system — you can tag characters by series, studio, genre, or year and automatically generate archive pages.</p>
<h2>Step 2: Set Up Hugo for Your Fan Site</h2>
<p>Install Hugo on your local machine (<code>brew install hugo</code> on macOS or download from the Hugo website). Create a new site: <code>hugo new site my-anime-site</code>. Install a theme by cloning a Git repository into the <code>themes</code> directory and adding it to your config. Create content: <code>hugo new characters/naruto-uzumaki.md</code>. Edit the Markdown file with your character information. Add front matter for structured data like series, abilities, and voice actors. Hugo's shortcodes let you embed images, YouTube trailers, and related character links. Build the site with <code>hugo</code> — the output goes to the <code>public</code> directory ready for deployment.</p>
<h2>Step 3: Deploy to GitHub Pages or Cloudflare Pages</h2>
<p>Push your Hugo site repository to GitHub. In the repository settings, enable GitHub Pages from the <code>gh-pages</code> branch (or use GitHub Actions to build and deploy automatically). For Cloudflare Pages, connect your Git repository and set the build command to <code>hugo</code> with an output directory of <code>public</code>. Cloudflare Pages provides preview URLs for each branch and automatic SSL. Both platforms support custom domains. Cloudflare Pages offers the advantage of unlimited bandwidth and tighter integration with Cloudflare DNS (which powers is-cool-me), making it the recommended choice for Hugo sites.</p>
<h2>Step 4: Connect Your is-cool-me Subdomain</h2>
<p>In the is-cool-me DNS dashboard, add a CNAME record pointing your subdomain to your hosting platform. For GitHub Pages, point to <code>username.github.io</code>. For Cloudflare Pages, point to <code>project-name.pages.dev</code>. Save the record. Then in your hosting platform's settings, add your is-cool-me subdomain as a custom domain. Both platforms will provision an SSL certificate automatically. For GitHub Pages, you may need to wait up to 20 minutes for the Let's Encrypt certificate. For Cloudflare Pages, the certificate is instant since it uses Cloudflare's own edge certificates.</p>
<h2>Step 5: Optimize Images for Fast Loading</h2>
<p>Anime fan sites often have many images — character art, screenshots, and fan creations. Optimize every image before uploading. Use tools like Squoosh, TinyPNG, or ImageMagick to reduce file sizes. Aim for JPEG quality 80-85 for photos and PNG-8 for illustrations with limited colors. Consider using WebP format for supported browsers (which covers over 95% of modern traffic). Hugo supports image processing — you can resize, crop, and convert images at build time using the <code>image</code> shortcode. Use lazy loading (<code>loading=&quot;lazy&quot;</code> attribute) for images below the fold to improve initial page load time.</p>
<h2>Best Practices</h2>
<ul><li>Organize content by series, genre, or character type using Hugo's taxonomy system for easy navigation</li><li>Add an RSS feed so visitors can subscribe to new content updates</li><li>Include a search feature — Hugo integrates with Lunr.js or Pagefind for client-side search without a backend</li><li>Credit your sources properly when using fan art or information from other databases</li><li>Add a sitemap.xml for search engines and register your subdomain with Google Search Console</li></ul>
<h2>Conclusion</h2>
<p>An anime fan site or wiki hosted on an is-cool-me subdomain combines passion with practicality. Hugo handles content management and static generation, GitHub Pages or Cloudflare Pages provides free hosting with CDN, and your branded subdomain gives the site a professional URL. The entire setup costs nothing while delivering fast load times and reliable uptime.</p>
<h2>FAQ</h2>
<p><strong>Can I use MediaWiki instead of Hugo for my fan wiki?</strong></p>
<p>Yes, but MediaWiki requires PHP and MySQL hosting, which is harder to find for free. Consider using a free PHP hosting provider or a low-cost VPS if you need full wiki functionality with user accounts.</p>
<p><strong>How do I handle copyrighted anime images on my fan site?</strong></p>
<p>Use images that are officially released promotional material, screenshots for educational purposes (review/analysis), or your own fan art. Always credit the original source and studio.</p>
<p><strong>Can I add a forum or comments section to my static site?</strong></p>
<p>Yes, you can embed third-party comment systems like Disqus or Remark42, or create a separate discussion space using free tools like GitHub Discussions.</p>`,

  'discord-bot-hosting-guide': `<h2>Introduction</h2>
<p>Discord bots have become essential tools for server management, entertainment, and community engagement. While the bot itself runs on your server or cloud platform, exposing its dashboard, webhook endpoints, or status page through a clean subdomain makes it accessible and professional. An is-cool-me subdomain gives your Discord bot a branded web presence with SSL, DNS management, and uptime monitoring — all for free. This guide covers hosting bot dashboards, webhook receivers, and status pages on your subdomain.</p>
<h2>Prerequisites</h2>
<ul><li>A Discord bot created in the Discord Developer Portal with a token</li><li>A hosting platform for your bot: a VPS, cloud server, or a free platform like Railway or Fly.io</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Basic knowledge of Node.js or Python for bot development</li></ul>
<h2>Step 1: Host Your Bot Dashboard on a Subdomain</h2>
<p>Many Discord bots have a web dashboard for configuration — slash command settings, moderation logs, role management, and analytics. Build your dashboard as a web application using React, Vue, or plain HTML/CSS. Host it on GitHub Pages, Vercel, or Cloudflare Pages. Connect your is-cool-me dashboard subdomain (e.g., <code>bot.yourname.is-pro.dev</code>) to the hosting platform using a CNAME record. The dashboard communicates with your bot via API endpoints exposed through the same subdomain or a dedicated API subdomain. Use OAuth2 with Discord to authenticate users on your dashboard — this allows users to log in with their Discord account and manage bot settings for servers they administer.</p>
<h2>Step 2: Set Up Webhook Endpoints</h2>
<p>Discord bots often need to receive webhooks from external services — GitHub, Twitter, payment processors, or monitoring tools. Create a webhook receiver endpoint on your bot server. Use Express.js for Node.js or Flask for Python to handle POST requests at a route like <code>/webhook/github</code>. Validate incoming webhooks using the provider's signature header to ensure requests are authentic. Your is-cool-me subdomain serves as the base URL for these webhooks. Configure your external services to send webhooks to <code>https://bot.yourname.is-pro.dev/webhook/github</code>. Make sure the DNS record for this subdomain is set to DNS-only (gray cloud) if your bot server uses a non-standard port, or use Cloudflare Tunnel for proxied connections.</p>
<h2>Step 3: Create a Bot Status Page</h2>
<p>A status page lets your users check if the bot is online, see recent incidents, and subscribe to notifications. Build a simple status page that pings your bot's API every 30 seconds and displays the result. Use green/yellow/red indicators for online, degraded, and offline states. Include a history of uptime checks and a subscribe form for email or Discord DM notifications. Host this status page on a separate is-cool-me subdomain like <code>status.yourname.is-pro.dev</code>. Use free uptime monitoring services like UptimeRobot or Better Stack to check your bot's API endpoint and trigger the status page update. This separate subdomain ensures your status page stays accessible even if the main bot dashboard goes down.</p>
<h2>Step 4: Configure SSL and DNS for Bot Services</h2>
<p>All bot web services should use HTTPS. Cloudflare provides free SSL certificates for proxied DNS records, which works well for bot dashboards and status pages hosted on standard web hosting platforms. For bot servers that run on custom ports (not 80/443), use DNS-only (gray cloud) mode and configure SSL directly on your server using Let's Encrypt with Certbot. If your bot server is behind a NAT or firewall without a public IP, use Cloudflare Tunnel (<code>cloudflared</code>) to expose your local bot services through Cloudflare's network. This provides a proxied connection with SSL even for services on non-standard ports, without needing to open firewall ports.</p>
<h2>Step 5: Set Up Uptime Monitoring</h2>
<p>Discord bots must maintain a WebSocket connection to Discord's gateway. If the bot goes offline, it stops responding to commands. Set up monitoring for both the bot itself (WebSocket connection health) and its web services (HTTP endpoint availability). Use UptimeRobot or Better Stack to ping your bot's API or status page every 5 minutes. Configure alerts via email, Discord webhook (ironic but effective), or SMS for critical outages. The is-cool-me monitoring dashboard can also track your subdomain DNS resolution and SSL certificate status. For comprehensive monitoring, set up a health check endpoint in your bot that returns memory usage, command counts, and WebSocket latency.</p>
<h2>Best Practices</h2>
<ul><li>Rate limit webhook endpoints to prevent abuse — implement a sliding window rate limiter in your webhook receiver</li><li>Use environment variables for sensitive configuration like the Discord bot token and OAuth2 client secret</li><li>Log all incoming webhooks for debugging, but sanitize sensitive data before writing to logs</li><li>Implement a maintenance mode page on your dashboard subdomain for planned downtimes</li></ul>
<h2>Conclusion</h2>
<p>An is-cool-me subdomain provides the perfect web companion for your Discord bot. Whether you need a configuration dashboard, webhook receiver, status page, or all three, the combination of free DNS management, SSL, and hosting creates a professional presence for your bot project.</p>
<h2>FAQ</h2>
<p><strong>Can I host the Discord bot itself on my subdomain?</strong></p>
<p>Not directly — a subdomain points to a web server, but Discord bots are WebSocket clients that connect to Discord's gateway. Use the subdomain for the bot's web dashboard and API, not the bot process itself.</p>
<p><strong>What if my bot server is on my home network with a dynamic IP?</strong></p>
<p>Use Cloudflare Tunnel (cloudflared) to create a secure, proxied connection without exposing your home IP. The tunnel handles SSL termination and works with dynamic IPs.</p>
<p><strong>How do I handle Discord OAuth2 callbacks on my subdomain?</strong></p>
<p>Register your subdomain URL (e.g., <code>https://bot.is-pro.dev/auth/callback</code>) in the Discord Developer Portal as a redirect URI. The OAuth2 flow will redirect users to this URL after authorization.</p>`,

  'static-site-generators-comparison': `<h2>Introduction</h2>
<p>Static site generators (SSGs) have revolutionized how developers build websites by combining the flexibility of modern development workflows with the speed and security of static HTML. The choice of SSG affects your development experience, build times, hosting options, and long-term maintainability. This guide compares three of the most popular SSGs — Hugo, Eleventy, and Astro — to help you choose the right tool for your is-cool-me subdomain project.</p>
<h2>Prerequisites</h2>
<ul><li>Basic familiarity with the command line and Git</li><li>A code editor (VS Code, Sublime Text, or similar)</li><li>An is-cool-me subdomain to deploy your site</li><li>Understanding of HTML, CSS, and basic templating concepts</li></ul>
<h2>Hugo: Speed Above All</h2>
<p>Hugo is the fastest static site generator, capable of building thousands of pages in seconds. Written in Go, it has no runtime dependencies — you download a single binary and start building. Hugo's template system uses Go templates, which have a learning curve but offer powerful features like shortcodes, content adapters, and menu management. Hugo excels at content-heavy sites: documentation projects, blogs with hundreds of posts, and large corporate sites. Its built-in asset pipeline handles SCSS/SASS compilation, image processing, JavaScript bundling, and fingerprinting without third-party tools. The theme ecosystem is mature, with hundreds of free and premium themes. Hugo's content organization is file-based using front matter in Markdown, with taxonomies for categorizing content. The main downside is that Go templates can be verbose, and Hugo's configuration is YAML-based with less community support for non-standard setups.</p>
<h2>Eleventy: Simplicity and Flexibility</h2>
<p>Eleventy (11ty) prides itself on being a simpler alternative to more complex SSGs. It does not impose a specific template language — you can use Nunjucks, Liquid, Handlebars, EJS, Pug, or plain HTML, even mixing them within the same project. This flexibility makes Eleventy an excellent choice for developers transitioning from different frameworks. Eleventy outputs zero client-side JavaScript by default, keeping your sites lean and fast. Its configuration is JavaScript-based (an <code>.eleventy.js</code> file), giving you full programmatic control over collections, filters, and build processes. Eleventy's ecosystem supports plugins for RSS feeds, sitemaps, image optimization, and syntax highlighting. It handles small to medium-sized sites well, but build times can become noticeable beyond a few thousand pages. Eleventy is ideal for portfolios, simple blogs, and marketing sites where simplicity and output quality matter more than build speed.</p>
<h2>Astro: Component-Based Architecture</h2>
<p>Astro represents the next generation of static site generators, combining a component-based architecture with zero-JS output by default. You build pages using Astro components (which support JSX-like syntax), and Astro renders them to static HTML at build time. What sets Astro apart is its ability to use components from React, Vue, Svelte, or Solid.js within the same project — you can import a React component alongside a Svelte component, and Astro handles the rendering. This makes Astro perfect for teams migrating from a specific framework or projects that need to reuse existing components. Astro's islands architecture lets you selectively hydrate interactive components on the client while keeping the rest of the page static. It supports Markdown and MDX for content, has built-in image optimization, and provides RSS, sitemap, and SEO integrations. The main trade-off is that Astro is newer, so its plugin ecosystem is smaller than Hugo's or Eleventy's.</p>
<h2>Comparison by Use Case</h2>
<p>For a documentation-heavy project or blog with hundreds of posts, Hugo's build speed and mature feature set make it the clear winner. For a developer portfolio or simple marketing site where simplicity and output quality are priorities, Eleventy provides the most straightforward path from project start to deployment. For a site that needs interactive components from multiple frameworks (React + Vue together), or a team that already knows a component framework and wants to adopt a static site, Astro offers unique advantages. All three generators produce static HTML that can be hosted on any free platform — GitHub Pages, Vercel, Netlify, or Cloudflare Pages — paired with your is-cool-me subdomain.</p>
<h2>Deployment Considerations</h2>
<p>All three SSGs deploy the same way: you build the site locally (or via CI), generating an output folder (<code>public</code> for Hugo, <code>_site</code> for Eleventy, <code>dist</code> for Astro), and deploy that folder to your hosting platform. GitHub Actions, Vercel, Netlify, and Cloudflare Pages all support automatic builds triggered by Git pushes. Hugo builds complete in seconds even for large sites, reducing CI wait times. Eleventy and Astro take longer for larger sites but still build in under a minute for most projects. Configure your hosting platform's build command and output directory, connect your is-cool-me subdomain via a CNAME record, and your site is live with automatic SSL.</p>
<h2>Best Practices</h2>
<ul><li>Choose the SSG that matches your content structure and team skills rather than chasing the newest tool</li><li>Use Git-based content management — all three SSGs work well with headless CMS options like Forestry or Decap CMS</li><li>Optimize images at build time using each SSG's built-in image pipeline for faster page loads</li><li>Configure incremental builds for large sites to reduce build times during development</li></ul>
<h2>Conclusion</h2>
<p>Hugo, Eleventy, and Astro each excel in different scenarios. Hugo offers unmatched speed for content-heavy sites, Eleventy provides simplicity and template flexibility, and Astro delivers a modern component architecture with multi-framework support. All three pair perfectly with an is-cool-me subdomain and free hosting platforms, giving you a professional web presence without infrastructure costs.</p>
<h2>FAQ</h2>
<p><strong>Can I switch between SSGs after my site is already built?</strong></p>
<p>Yes, since all three output static HTML, you can migrate content and rebuild with a different SSG. The effort depends on how your content is structured — Markdown front matter is broadly compatible.</p>
<p><strong>Which SSG has the best image optimization?</strong></p>
<p>Astro and Hugo both have excellent built-in image processing. Eleventy requires a plugin (eleventy-img) for image optimization, which is well-maintained and powerful.</p>
<p><strong>Do I need to know JavaScript to use these SSGs?</strong></p>
<p>Hugo does not require JavaScript — everything is configured with YAML/TOML and Go templates. Eleventy and Astro use JavaScript for configuration but the templates can be HTML-only or use template languages that do not require JS knowledge.</p>`,

  'custom-email-address-for-domain': `<h2>Introduction</h2>
<p>A custom email address like <code>hello@yourname.is-pro.dev</code> signals professionalism and brand ownership. It tells recipients that you take your online presence seriously. With is-cool-me's Cloudflare DNS integration, you can set up custom email forwarding at no cost using Cloudflare Email Routing or other free forwarding services. This guide covers everything from configuring MX records to setting up SPF, DKIM, and DMARC for reliable email delivery.</p>
<h2>Prerequisites</h2>
<ul><li>An is-cool-me subdomain with DNS managed through Cloudflare</li><li>A Cloudflare account (free tier) with access to Email Routing</li><li>A personal email inbox (Gmail, Outlook, ProtonMail) to receive forwarded messages</li></ul>
<h2>Step 1: Enable Cloudflare Email Routing</h2>
<p>Log into your Cloudflare account and select your is-pro.dev zone. Navigate to "Email &rarr; Email Routing" and click "Get started". Cloudflare Email Routing is free for up to 100 email addresses per zone. It forwards incoming emails to your destination inbox without storing messages on Cloudflare's servers. The setup requires adding specific MX records and optionally SPF and DKIM records. Cloudflare walks you through each step with a guided setup wizard. Once Email Routing is enabled, you can create custom email addresses that forward to any existing inbox.</p>
<h2>Step 2: Configure MX Records</h2>
<p>Cloudflare Email Routing requires specific MX records to function. In the Email Routing dashboard, Cloudflare will show you the required MX record values. Typically, these are: <code>route1.mx.cloudflare.net</code> with priority 10 and <code>route2.mx.cloudflare.net</code> with priority 20. In the is-cool-me DNS dashboard (which is your Cloudflare DNS), these records may be added automatically when you enable Email Routing. Verify they exist in the DNS settings. If not, add them manually. Ensure the MX records are set to DNS-only (gray cloud) — proxied MX records break email delivery. The priority 10 route is the primary mail server; the priority 20 route serves as a backup if the first is unavailable.</p>
<h2>Step 3: Create Email Addresses and Set Up Forwarding</h2>
<p>Back in Cloudflare Email Routing, go to the "Addresses" tab. Click "Create address". Enter your desired email prefix (e.g., <code>hello</code> for <code>hello@yourname.is-pro.dev</code>). Then enter the destination email address where forwarded messages should arrive — this can be a Gmail, Outlook, or any other email address. Click "Save". Repeat for any other addresses you need (<code>support@</code>, <code>admin@</code>, etc.). You can set up catch-all forwarding (all <code>anything@yourname.is-pro.dev</code> addresses) from the settings, but use catch-all sparingly as it increases spam volume. Test forwarding by sending an email to your new custom address — it should arrive in your destination inbox within seconds.</p>
<h2>Step 4: Add SPF Record to Prevent Spoofing</h2>
<p>An SPF (Sender Policy Framework) TXT record tells receiving email servers which servers are authorized to send email on behalf of your domain. Without SPF, spammers can forge emails that appear to come from your subdomain. In your DNS dashboard, add a TXT record for your subdomain with the value: <code>v=spf1 include:_spf.mx.cloudflare.net ~all</code>. The <code>include:_spf.mx.cloudflare.net</code> authorizes Cloudflare's mail servers, and <code>~all</code> marks unauthenticated email as "soft fail" (receiver can quarantine it). For stricter security, use <code>-all</code> (hard fail) which tells receivers to reject unauthenticated email. However, test with <code>~all</code> first to avoid accidentally blocking legitimate email.</p>
<h2>Step 5: Add DKIM and DMARC Records</h2>
<p>DKIM (DomainKeys Identified Mail) adds a cryptographic signature to outgoing emails, allowing receivers to verify the email was not tampered with. In Cloudflare Email Routing, go to "Settings &rarr; DKIM". Generate a DKIM key and Cloudflare will provide a TXT record to add to your DNS. This record has a selector name (e.g., <code>cloudflare._domainkey</code>) and a long public key value. Add it to your DNS and enable DKIM signing. DMARC (Domain-based Message Authentication, Reporting & Conformance) builds on SPF and DKIM. Add a DMARC TXT record for <code>_dmarc.yourname.is-pro.dev</code> with a value like: <code>v=DMARC1; p=quarantine; rua=mailto:dmarc-reports@yourname.is-pro.dev</code>. This tells receivers to quarantine emails that fail SPF or DKIM checks and send aggregate reports to your email.</p>
<h2>Step 6: Test Email Delivery</h2>
<p>After configuring DNS records, test your email setup thoroughly. Send emails to your custom address from different providers (Gmail, Outlook, Yahoo) and confirm they arrive. Check the email headers of received messages to verify SPF (<code>Received-SPF: Pass</code>), DKIM (<code>dkim=pass</code>), and DMARC (<code>dmarc=pass</code>) are all passing. Use online email testing tools like Mail-tester.com to get a comprehensive score (aim for 10/10). If you send emails from your subdomain (not just receive), configure your email client to send through a service that supports your custom domain, such as Gmail's "Send mail as" feature, which requires SMTP configuration.</p>
<h2>Best Practices</h2>
<ul><li>Create separate email addresses for different purposes: <code>hello@</code> for general inquiries, <code>security@</code> for security reports, <code>abuse@</code> for abuse complaints</li><li>Monitor DMARC reports to detect unauthorized email sending attempts</li><li>Avoid catch-all forwarding unless necessary — it reduces spam filtering accuracy</li><li>Register a separate subdomain just for email if you want to keep email infrastructure isolated from your main site</li></ul>
<h2>Conclusion</h2>
<p>Setting up custom email on your is-cool-me subdomain is free and straightforward with Cloudflare Email Routing. MX records route incoming mail, SPF prevents spoofing, DKIM provides cryptographic verification, and DMARC ties everything together with a reporting policy. The result is a professional email address that enhances your brand credibility without ongoing costs.</p>
<h2>FAQ</h2>
<p><strong>Can I send email from my custom subdomain, or just receive?</strong></p>
<p>Cloudflare Email Routing only handles incoming email (forwarding). To send from your custom address, configure Gmail, Outlook, or another email client to send through an SMTP server that supports your domain.</p>
<p><strong>How many emails can I forward per day on the free tier?</strong></p>
<p>Cloudflare Email Routing free tier forwards up to 100 emails per day. For higher volumes, consider an email forwarding service or a paid email hosting solution.</p>
<p><strong>Why are my forwarded emails going to spam?</strong></p>
<p>This is usually an SPF or DKIM configuration issue. Verify your TXT records are published correctly and use an email testing tool to check your email authentication score.</p>`,

  'vps-deployment-with-ssh': `<h2>Introduction</h2>
<p>A Virtual Private Server (VPS) gives you full control over your hosting environment — custom software, root access, and dedicated resources. By connecting a VPS to an is-cool-me subdomain, you get a branded URL for your server with Cloudflare DNS management and optional DDoS protection. This guide covers the complete workflow: provisioning a VPS, setting up Nginx as a reverse proxy, securing it with SSL via Certbot, configuring your subdomain, and automating deployments with Git hooks.</p>
<h2>Prerequisites</h2>
<ul><li>A VPS from any provider (DigitalOcean, Linode, Vultr, Hetzner, or AWS EC2) with Ubuntu 22.04 or 24.04</li><li>SSH access to your VPS with a root or sudo-enabled user</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Basic familiarity with Linux command line and SSH</li></ul>
<h2>Step 1: Initial Server Setup</h2>
<p>SSH into your VPS: <code>ssh root@your-server-ip</code>. First, update the package manager: <code>apt update && apt upgrade -y</code>. Create a non-root user with sudo privileges: <code>adduser deploy</code> and <code>usermod -aG sudo deploy</code>. Set up SSH key authentication by copying your public key to the new user's <code>~/.ssh/authorized_keys</code>. Disable password authentication in <code>/etc/ssh/sshd_config</code> by setting <code>PasswordAuthentication no</code>, then restart SSH: <code>systemctl restart ssh</code>. Configure a basic firewall: <code>ufw allow OpenSSH && ufw allow 'Nginx Full' && ufw enable</code>. This ensures only SSH and web traffic can reach your server. Install Nginx: <code>apt install nginx -y</code>.</p>
<h2>Step 2: Configure Nginx as a Reverse Proxy</h2>
<p>Create a new Nginx server block for your subdomain: <code>nano /etc/nginx/sites-available/yourname.is-pro.dev</code>. Configure it as a reverse proxy to your application. For example, if your app runs on port 3000: <code>server { listen 80; server_name yourname.is-pro.dev; location / { proxy_pass http://127.0.0.1:3000; proxy_set_header Host \$host; proxy_set_header X-Real-IP \$remote_addr; proxy_set_header X-Forwarded-For \$proxy_add_x_forwarded_for; proxy_set_header X-Forwarded-Proto \$scheme; } }</code>. Enable the site: <code>ln -s /etc/nginx/sites-available/yourname.is-pro.dev /etc/nginx/sites-enabled/</code>. Test the configuration: <code>nginx -t</code>. If OK, reload Nginx: <code>systemctl reload nginx</code>. Your application should now be accessible over HTTP at your VPS's IP address.</p>
<h2>Step 3: Configure DNS in is-cool-me Dashboard</h2>
<p>In the is-cool-me DNS dashboard, add an A record pointing your subdomain to your VPS's IPv4 address. Add an AAAA record if your VPS has an IPv6 address. Set TTL to 60 seconds for initial testing. Important: for a VPS, you can use either proxied (orange cloud) or DNS-only (gray cloud) mode. Proxied mode hides your VPS IP and provides DDoS protection, but if your application uses WebSockets or custom protocols, you may need DNS-only mode or configure Cloudflare to allow WebSocket traffic. For most web applications with Nginx behind Cloudflare, proxied mode works well and adds security benefits.</p>
<h2>Step 4: Obtain SSL Certificate with Certbot</h2>
<p>Install Certbot and the Nginx plugin: <code>apt install certbot python3-certbot-nginx -y</code>. Set proxy mode to DNS-only (temporarily) for certificate issuance: run <code>certbot --nginx -d yourname.is-pro.dev</code>. Certbot will automatically modify your Nginx configuration to use HTTPS. Follow the prompts to set up auto-renewal. After the certificate is issued, you can switch the DNS record back to proxied (orange cloud) mode if desired. Certbot sets up a systemd timer for automatic renewal — verify it with <code>systemctl status certbot.timer</code>. If you keep the record proxied through Cloudflare, you can use Cloudflare's Origin CA certificate instead of Let's Encrypt for end-to-end encryption.</p>
<h2>Step 5: Set Up Automatic Deployments with Git Hooks</h2>
<p>Create a bare Git repository on your VPS for your application: <code>mkdir -p ~/app.git && cd ~/app.git && git init --bare</code>. Create a post-receive hook: <code>nano hooks/post-receive</code>. Add the following script: <code>#!/bin/bash; GIT_WORK_TREE=/var/www/yourname git checkout -f; cd /var/www/yourname && npm install && pm2 restart app</code>. Make it executable: <code>chmod +x hooks/post-receive</code>. On your local machine, add the VPS as a remote: <code>git remote add deploy ssh://deploy@your-server-ip/home/deploy/app.git</code>. Deploy by running <code>git push deploy main</code>. The Git hook checks out the latest code, installs dependencies, and restarts your application. This gives you a CI/CD-like workflow without external services.</p>
<h2>Step 6: Monitor and Maintain</h2>
<p>Set up monitoring for your VPS: install <code>netdata</code> or <code>htop</code> for real-time resource monitoring, configure log rotation for Nginx and application logs, set up automated backups with <code>rsync</code> or <code>rclone</code> to offsite storage, and configure fail2ban to block brute-force SSH attempts. Keep your server updated with automatic security updates: <code>apt install unattended-upgrades && dpkg-reconfigure -plow unattended-upgrades</code>. Set up uptime monitoring for your subdomain using the is-cool-me monitoring tools or external services like UptimeRobot.</p>
<h2>Best Practices</h2>
<ul><li>Use SSH keys only (disable password authentication) for server access</li><li>Run applications as a non-root user with limited permissions</li><li>Keep Nginx, Node.js, and all application dependencies updated</li><li>Use environment variables for sensitive configuration — never hardcode credentials</li><li>Set up swap space (2GB minimum) for memory-intensive applications</li></ul>
<h2>Conclusion</h2>
<p>A VPS paired with an is-cool-me subdomain gives you full control over your hosting stack. Nginx handles reverse proxying and static file serving, SSL certificates from Certbot encrypt traffic, Git hooks automate deployments, and your branded subdomain provides a professional URL. This setup is ideal for applications that need more than static hosting can provide.</p>
<h2>FAQ</h2>
<p><strong>How much does a VPS cost compared to shared hosting?</strong></p>
<p>VPS plans start around $4-6/month from providers like DigitalOcean, Vultr, or Hetzner. Shared hosting is cheaper but offers less control and performance.</p>
<p><strong>Should I use Cloudflare proxied mode or DNS-only for my VPS?</strong></p>
<p>Use proxied mode for web applications (HTTP/HTTPS) to get DDoS protection and IP hiding. Use DNS-only for non-HTTP services like SSH, game servers, or custom protocol applications.</p>
<p><strong>What if my VPS IP changes?</strong></p>
<p>Update the A record in your is-cool-me DNS dashboard with the new IP. With Cloudflare DNS, propagation is nearly instant. Consider using a dynamic DNS service if your VPS provider frequently changes your IP.</p>`,
  'shopify-store-subdomain': `<h2>Introduction</h2>
<p>Shopify is one of the most popular e-commerce platforms, powering millions of online stores worldwide. By connecting an is-cool-me subdomain to your Shopify store, you get a branded URL for your storefront that is easy to share and remember. This guide covers the DNS configuration required to point your subdomain to Shopify, set up SSL for secure checkout, and ensure your store runs smoothly on the custom domain.</p>
<h2>Prerequisites</h2>
<ul><li>An active Shopify subscription (trial or paid) with a published store</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Access to the is-cool-me DNS dashboard (Cloudflare-powered)</li></ul>
<h2>Step 1: Configure Shopify for Your Custom Domain</h2>
<p>Log into your Shopify admin panel. Navigate to "Settings &rarr; Domains" and click "Connect existing domain". Enter your is-cool-me subdomain (e.g., <code>store.is-pro.dev</code>). Shopify will verify domain ownership by checking for a specific TXT record that you need to add to your DNS. This verification step ensures you actually control the domain. Copy the verification TXT record value provided by Shopify. Do not proceed until you have this value ready — you will add it in the next step.</p>
<h2>Step 2: Add DNS Verification Record</h2>
<p>In the is-cool-me DNS dashboard, add a TXT record for your subdomain. The name should be your subdomain prefix (e.g., <code>store</code>) and the value should be the verification string from Shopify. Shopify verification TXT records typically start with <code>shopify-verification=</code> followed by a long alphanumeric string. Set TTL to 60 seconds. Save the record. Return to Shopify and click "Verify" in the Domains settings page. Shopify checks for the TXT record and confirms ownership. Once verified, you can keep the TXT record or remove it — Shopify does not require it for ongoing operation.</p>
<h2>Step 3: Point Your Subdomain to Shopify</h2>
<p>After verification, Shopify displays the DNS records needed to point your subdomain to Shopify's infrastructure. For a subdomain, you typically need a CNAME record pointing to <code>shops.myshopify.com</code>. In the is-cool-me DNS dashboard, add a CNAME record: enter your subdomain prefix as the name and <code>shops.myshopify.com</code> as the target. Important: set the proxy status to DNS-only (gray cloud). Shopify requires direct traffic for its CDN and SSL to function correctly with their custom domain feature. Proxied mode (orange cloud) can interfere with Shopify's certificate validation and cause checkout errors. Save the record.</p>
<h2>Step 4: Configure SSL for Your Store</h2>
<p>Shopify automatically provisions an SSL certificate for custom domains through Let's Encrypt. This process begins once the DNS CNAME record resolves to Shopify's servers. It typically takes 5-15 minutes but can take up to 48 hours in rare cases. During this period, visitors may see a security warning. Shopify manages the entire SSL lifecycle — the certificate is automatically renewed, and you do not need to take any action. Once the SSL certificate is active, Shopify enables HTTPS for your domain and processes all traffic securely, which is essential for PCI compliance when accepting payments through your store.</p>
<h2>Step 5: Set Up the Primary Domain</h2>
<p>In Shopify's Domains settings, you can set your is-cool-me subdomain as the primary domain. This means all storefront links, email notifications, and search engine references will use this domain. Shopify also supports redirecting the <code>www</code> variant. If you want both <code>store.is-pro.dev</code> and <code>www.store.is-pro.dev</code> to work, add a CNAME record for <code>www.store</code> pointing to <code>shops.myshopify.com</code> and add <code>www.store.is-pro.dev</code> in Shopify's domain settings. Shopify will automatically redirect one to the other based on your primary domain setting.</p>
<h2>Step 6: Test the Complete Setup</h2>
<p>Thoroughly test your store on the custom domain. Visit your subdomain and verify the homepage loads over HTTPS. Navigate through product pages, add items to the cart, and proceed through the checkout to the payment page (you do not need to complete the purchase). Confirm the browser padlock icon appears and certificate details show it is issued to your subdomain. Test on mobile devices and different browsers. Verify that email notifications from Shopify (order confirmations, shipping updates) contain your custom subdomain URL. Use the is-cool-me HTTP Headers Checker to verify security headers are properly set.</p>
<h2>Best Practices</h2>
<ul><li>Keep the Shopify TXT verification record in your DNS in case you need to re-verify the domain in the future</li><li>Use a short, memorable subdomain name like <code>shop.is-pro.dev</code> or <code>store.is-pro.dev</code></li><li>Monitor your SSL certificate status using the SSL Checker tool on is-cool-me</li><li>If you use Cloudflare proxied mode for other services, keep the Shopify record DNS-only to avoid conflicts</li></ul>
<h2>Conclusion</h2>
<p>Connecting a Shopify store to an is-cool-me subdomain gives you a branded, professional storefront URL without the cost of registering a separate domain. The DNS configuration is minimal — a TXT record for verification and a CNAME record for routing — and Shopify handles SSL automatically. Your customers get a seamless, secure shopping experience on your custom subdomain.</p>
<h2>FAQ</h2>
<p><strong>Can I use a subdomain like shop.is-pro.dev with Shopify?</strong></p>
<p>Yes, Shopify supports subdomain connections just like root domains. The configuration is identical — you set up the subdomain as a custom domain in Shopify settings.</p>
<p><strong>Why is my Shopify site showing a "Domain not configured" error?</strong></p>
<p>This means the DNS record has not propagated to Shopify's servers yet, or the CNAME record is incorrect. Verify the CNAME target in your DNS dashboard matches Shopify's required value exactly.</p>
<p><strong>Does Shopify support email hosting with custom domains?</strong></p>
<p>No, Shopify does not provide email hosting. Use Cloudflare Email Routing or a third-party email service for custom email addresses on your subdomain.</p>`,

  'wordpress-on-free-subdomain': `<h2>Introduction</h2>
<p>WordPress powers over 40% of all websites on the internet, making it the most popular content management system in the world. While WordPress itself is free, hosting typically costs money. By combining a free is-cool-me subdomain with free or low-cost PHP hosting, you can run a WordPress site for little to no cost. This guide covers deployment options including free PHP hosting services, VPS setups, and the DNS configuration needed to connect your subdomain to WordPress.</p>
<h2>Prerequisites</h2>
<ul><li>A WordPress installation ready to deploy (downloaded from wordpress.org)</li><li>A MySQL or MariaDB database for WordPress data storage</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Basic knowledge of PHP, MySQL, and web server configuration</li></ul>
<h2>Step 1: Choose a Hosting Option</h2>
<p>There are several ways to host WordPress on a free or low budget. Option one: use a free PHP hosting provider like 000webhost, InfinityFree, or FreeHosting.com. These platforms offer pre-installed WordPress or manual installation via cPanel and Softaculous. Their free tiers include PHP and MySQL support but come with limitations: limited storage (typically 250MB-1GB), forced ads on some providers, and lower performance. Option two: deploy WordPress on a low-cost VPS ($4-6/month) using a LAMP/LEMP stack installation script. Option three: use WordPress.com's free tier, which gives you a subdomain but limits customization and requires a paid upgrade for custom domains. For maximum control and zero ads, a self-hosted WordPress on a low-cost VPS with an is-cool-me subdomain is the best combination.</p>
<h2>Step 2: Install WordPress on Your Server</h2>
<p>For a VPS setup, install a LAMP (Linux, Apache, MySQL, PHP) stack. Run: <code>apt install apache2 mysql-server php php-mysql php-curl php-gd php-mbstring php-xml php-zip -y</code>. Create a MySQL database and user for WordPress: <code>mysql -u root -p -e "CREATE DATABASE wordpress; CREATE USER 'wpuser'@'localhost' IDENTIFIED BY 'password'; GRANT ALL PRIVILEGES ON wordpress.* TO 'wpuser'@'localhost'; FLUSH PRIVILEGES;"</code>. Download WordPress: <code>cd /var/www/html && wget https://wordpress.org/latest.tar.gz && tar xzf latest.tar.gz && mv wordpress/* . && rm -rf wordpress latest.tar.gz</code>. Set permissions: <code>chown -R www-data:www-data /var/www/html</code>. Run the WordPress web installer by visiting your VPS IP address in a browser and following the setup wizard.</p>
<h2>Step 3: Configure Nginx or Apache for WordPress</h2>
<p>Configure your web server for WordPress. For Nginx, create a server block: <code>server { listen 80; server_name yourname.is-pro.dev; root /var/www/wordpress; index index.php; location / { try_files \$uri \$uri/ /index.php?\$args; } location ~ \.php\$ { include fastcgi_params; fastcgi_pass unix:/var/run/php/php8.1-fpm.sock; fastcgi_param SCRIPT_FILENAME \$document_root\$fastcgi_script_name; } }</code>. Enable the site and reload Nginx. For Apache, the default virtual host configuration usually works after pointing the document root to the WordPress directory. Enable the rewrite module: <code>a2enmod rewrite</code> and restart Apache. WordPress requires the <code>mod_rewrite</code> module for clean permalinks, which are important for SEO.</p>
<h2>Step 4: Configure DNS in is-cool-me Dashboard</h2>
<p>In the is-cool-me DNS dashboard, add an A record pointing your WordPress subdomain to your server's IP address. If you are using a free PHP hosting provider, use a CNAME record pointing to the provider's server domain. For VPS setups, use proxied mode (orange cloud) to hide your server IP and provide DDoS protection. For free hosting providers, use DNS-only (gray cloud) since the provider manages its own SSL. WordPress expects to receive traffic on standard HTTP/HTTPS ports, so proxied mode works well in most cases.</p>
<h2>Step 5: Install SSL Certificate</h2>
<p>WordPress sites must use HTTPS to avoid browser security warnings. For VPS setups, use Certbot to install a Let's Encrypt certificate: <code>apt install certbot python3-certbot-nginx -y && certbot --nginx -d yourname.is-pro.dev</code>. For free PHP hosting providers, check if they offer free SSL certificates via AutoSSL or Let's Encrypt integration in cPanel. If you use Cloudflare proxied mode, the edge certificate provides SSL automatically, but configure WordPress to detect HTTPS: install a plugin like "Really Simple SSL" or add <code>define('FORCE_SSL_ADMIN', true);</code> to your <code>wp-config.php</code>. Without proper HTTPS detection, WordPress may load resources over HTTP, triggering mixed content warnings.</p>
<h2>Step 6: Optimize and Secure WordPress</h2>
<p>After WordPress is installed, optimize it for performance and security. Install caching plugins like W3 Total Cache or WP Super Cache. Configure WordPress permalinks to use a clean structure (e.g., <code>/post-name/</code>). Install a security plugin like Wordfence or Sucuri for firewall protection. Change the default <code>wp_</code> database table prefix. Limit login attempts and enforce strong passwords. Set up automated WordPress core, theme, and plugin updates. Verify your WordPress site is responding correctly on your is-cool-me subdomain, with all internal links using the custom domain URL rather than the server IP address.</p>
<h2>Best Practices</h2>
<ul><li>Never use the default "admin" username — create a unique administrator username during installation</li><li>Keep WordPress core, themes, and plugins updated to patch security vulnerabilities</li><li>Use a child theme for customizations so updates do not overwrite your changes</li><li>Regularly back up your WordPress files and database — use a plugin like UpdraftPlus for automated backups</li></ul>
<h2>Conclusion</h2>
<p>Running WordPress on an is-cool-me subdomain gives you a powerful content management system with a professional URL. Whether you choose a free PHP hosting provider or a low-cost VPS, the combination of WordPress and your custom subdomain can support anything from a personal blog to a full-featured website, at minimal cost.</p>
<h2>FAQ</h2>
<p><strong>Can I use the WordPress.com free plan with a custom is-cool-me subdomain?</strong></p>
<p>WordPress.com's free plan does not support custom domains. You need at least the Personal plan ($4/month) to connect a custom subdomain.</p>
<p><strong>Is WordPress hosting on free PHP providers reliable?</strong></p>
<p>Free PHP hosting providers are adequate for low-traffic personal sites but often have performance limitations, forced ads, and less reliable uptime compared to paid options.</p>
<p><strong>How do I migrate an existing WordPress site to my is-cool-me subdomain?</strong></p>
<p>Export your existing WordPress content via Tools &rarr; Export in the admin panel. Install WordPress on your new host, then use Tools &rarr; Import to upload the exported file. Update the Site URL in Settings &rarr; General to your is-cool-me subdomain.</p>`,

  'game-server-subdomain-config': `<h2>Introduction</h2>
<p>Game servers typically run on non-standard ports, making them incompatible with standard web DNS configurations. SRV records solve this by allowing you to map a service name and protocol to a specific hostname and port. With an is-cool-me subdomain, you can give your game server a clean, memorable address like <code>mc.is-pro.dev</code> instead of asking players to remember <code>203.0.113.5:25565</code>. This guide covers configuring SRV records for Minecraft, Valheim, and other popular game servers.</p>
<h2>Prerequisites</h2>
<ul><li>A game server running and accessible on your network or VPS</li><li>Your server's public IP address or dynamic DNS hostname</li><li>An is-cool-me subdomain registered at dash.is-pro.dev</li><li>Knowledge of the port number your game server uses (e.g., 25565 for Minecraft)</li></ul>
<h2>Step 1: Understanding SRV Record Structure</h2>
<p>An SRV record has a specific format: <code>_service._protocol.name TTL class SRV priority weight port target</code>. For Minecraft Java Edition, the service is <code>_minecraft</code> and the protocol is <code>_tcp</code>. The name is your subdomain. Priority determines which server to try first (lower = higher priority). Weight distributes load among servers with the same priority. Port is the server's listening port. Target is the hostname (not IP) of the server. A complete Minecraft SRV record looks like: <code>_minecraft._tcp.mc.is-pro.dev 300 IN SRV 0 5 25565 yourserver.example.com</code>. The TTL can be set to 300-600 seconds for game servers since IP changes are infrequent. Note that the target must be a hostname, not an IP address — if your server only has an IP, use a dynamic DNS service or create an A record pointing a hostname to the IP, then use that hostname as the SRV target.</p>
<h2>Step 2: Configure SRV Records in is-cool-me Dashboard</h2>
<p>In the is-cool-me DNS dashboard, click "Add Record" and select SRV as the record type. The dashboard will show fields for Service, Protocol, Priority, Weight, Port, and Target. For a Minecraft Java Edition server: set Service to <code>minecraft</code>, Protocol to <code>TCP</code>, Priority to <code>0</code>, Weight to <code>5</code>, Port to <code>25565</code>, and Target to your server's hostname (e.g., <code>yourserver.example.com</code>). The system constructs the full SRV record name automatically. Set the record to DNS-only (gray cloud) — game server traffic uses non-HTTP protocols on non-standard ports and cannot be proxied through Cloudflare. Save the record and verify it appears in your record list.</p>
<h2>Step 3: Minecraft Java Edition Configuration</h2>
<p>Minecraft Java Edition automatically checks for SRV records when a player connects. If a player types <code>mc.is-pro.dev</code> into the multiplayer server address field, Minecraft looks up <code>_minecraft._tcp.mc.is-pro.dev</code> and redirects to your server IP and port. The server.properties file on your Minecraft server must set <code>server-ip=</code> to blank (to listen on all interfaces) and <code>server-port=25565</code>. If you change the port from 25565, update the SRV record port accordingly. For Minecraft Bedrock Edition, the SRV service is <code>_minecraft</code> but the protocol is <code>_udp</code> and the default port is 19132. Test the SRV resolution using an online Minecraft server status checker that supports SRV lookup.</p>
<h2>Step 4: Valheim and Other Game Server Configuration</h2>
<p>Valheim servers use different SRV record values. Service: <code>_valheim</code>, Protocol: <code>_udp</code>, Port: 2456 (default). Some games use multiple ports — Valheim uses 2456-2458. Configure your server to listen on these ports and add SRV records for the primary connection port. For other games, check the game's documentation for the correct service name, protocol (TCP vs UDP), and default port. Games that support SRV records include: Counter-Strike (source), Team Fortress 2, Quake, and many others. Not all games support SRV lookups — for unsupported games, you can only use an A/AAAA record pointing to your server IP and direct players to use <code>yourname.is-pro.dev:port</code> format.</p>
<h2>Step 5: Dynamic DNS for Residential Servers</h2>
<p>If you host a game server on your home network with a dynamic IP address, your SRV record's target hostname needs to update when your IP changes. Set up a dynamic DNS service: use Duck DNS (<code>yourserver.duckdns.org</code>) or Cloudflare's API to update an A record automatically when your IP changes. Your SRV record then points to the dynamic DNS hostname, and the dynamic DNS client keeps the A record updated. On the is-cool-me side, you only configure the SRV record once — it always points to your dynamic DNS hostname. Set up a cron job or use a Docker container that runs a dynamic DNS update script every 5 minutes.</p>
<h2>Step 6: Test SRV Resolution</h2>
<p>Use command-line tools to verify SRV records are configured correctly. Run <code>nslookup -type=srv _minecraft._tcp.mc.is-pro.dev</code> or <code>dig _minecraft._tcp.mc.is-pro.dev SRV</code>. The response should show your server's target hostname and port. Test actual game connectivity by launching the game and connecting using your subdomain. Check server logs to confirm connections are arriving. If players cannot connect, verify the SRV record format, ensure your server firewall allows the game port, and confirm port forwarding is configured on your router if hosting from home. Use online SRV lookup tools to double-check your DNS configuration from external networks.</p>
<h2>Best Practices</h2>
<ul><li>Set up monitoring for your game server using the is-cool-me uptime monitoring tools to detect crashes</li><li>Use multiple SRV records with different priorities for redundant game servers</li><li>Keep your server software updated to patch security vulnerabilities</li><li>Configure a firewall to allow only the necessary game ports and block everything else</li></ul>
<h2>Conclusion</h2>
<p>SRV records transform a complex game server address into a simple, memorable subdomain. With an is-cool-me subdomain, you configure the SRV record once and players connect using an easy-to-remember address. This setup works for Minecraft, Valheim, and many other games, making your server more accessible to players.</p>
<h2>FAQ</h2>
<p><strong>Do all games support SRV records?</strong></p>
<p>No. SRV record support depends on the game client. Minecraft Java Edition supports them natively. Many older games and console games do not. Check your game's documentation for SRV support.</p>
<p><strong>Can I use an SRV record with Cloudflare proxied mode?</strong></p>
<p>No, SRV records must always be DNS-only (gray cloud). Cloudflare's proxy only handles HTTP/HTTPS and WebSocket traffic, which game server protocols are not.</p>
<p><strong>What if my server IP changes?</strong></p>
<p>Use a dynamic DNS hostname as the SRV target. You only need to update the A record of the dynamic DNS hostname when your IP changes, not the SRV record itself.</p>`,

  'monitoring-subdomain-health': `<h2>Introduction</h2>
<p>Your is-cool-me subdomain may host critical services — a portfolio, API, or business site. When it goes down, you need to know immediately. Uptime monitoring services track your subdomain's availability, response time, and SSL certificate status, alerting you when something goes wrong. This guide covers setting up free monitoring using UptimeRobot, Better Stack, or the built-in is-cool-me monitoring tools, configuring alerts, and creating a public status page.</p>
<h2>Prerequisites</h2>
<ul><li>An is-cool-me subdomain with a live service (website, API, or application)</li><li>An account on UptimeRobot or Better Stack (free tier)</li><li>Access to your notification channels (email, Slack, Discord, or SMS)</li></ul>
<h2>Step 1: Choose a Monitoring Service</h2>
<p>Several free monitoring services offer reliable uptime tracking. UptimeRobot monitors 50 URLs on the free tier at 5-minute intervals, with alerts via email, SMS, Slack, Discord, and webhooks. Better Stack (formerly Better Uptime) offers 10 monitors on the free tier with status pages, incident management, and a 3-minute check interval. Both services check your subdomain from multiple global locations and report response times. The is-cool-me platform also provides built-in monitoring for registered subdomains, tracking DNS resolution, SSL status, and HTTP response codes. For most users, one of these free services provides sufficient coverage.</p>
<h2>Step 2: Configure Monitors for Your Subdomain</h2>
<p>In your chosen monitoring service, create a new monitor. Set the monitor type to "HTTP" or "HTTPS" for web services. Enter your full subdomain URL: <code>https://yourname.is-pro.dev</code>. Configure the check interval — UptimeRobot free tier offers 5-minute checks, which balances coverage with API usage limits. Set the timeout (typically 30 seconds) and select the monitoring locations closest to your target audience. For APIs, configure the monitor to check a specific endpoint and expect a particular HTTP status code (e.g., <code>200 OK</code>) or keyword in the response body. Add monitors for all critical subdomains — one for your main site, one for your API, and one for your status page.</p>
<h2>Step 3: Set Up Alert Notifications</h2>
<p>Configure alert contacts in your monitoring service. For email alerts, add your primary email address. For faster response, set up Slack or Discord webhook integrations — paste the webhook URL from your Slack or Discord channel into the monitoring service's integration settings. This posts instant notifications when downtime is detected. For SMS alerts, UptimeRobot offers 50 SMS credits per month on the free plan. Better Stack offers push notifications via their mobile app. Configure alert thresholds: "Down" after 2 consecutive failed checks to avoid false alarms from transient network issues. Set up "Up" notifications so you know when the service recovers.</p>
<h2>Step 4: Monitor SSL Certificate Status</h2>
<p>SSL certificate expiration is one of the most common causes of unexpected downtime. Configure your monitoring service to check SSL certificate validity and expiration date. Both UptimeRobot and Better Stack include SSL monitoring in their HTTP checks. Set a warning threshold — get notified 30, 14, and 7 days before certificate expiration. For is-cool-me subdomains with Cloudflare proxied mode, Cloudflare's Universal SSL certificates renew automatically, so manual renewal is not needed. For DNS-only records with self-managed Let's Encrypt certificates, monitoring is essential because Certbot auto-renewal can fail if ports are blocked or the ACME challenge fails.</p>
<h2>Step 5: Create a Public Status Page</h2>
<p>A public status page lets your users check if your service is experiencing issues without contacting you. Better Stack offers a free status page with the ability to mark incidents and show uptime history. Create a status page listing all your monitored services. Customize it with your branding and subdomain (e.g., <code>status.yourname.is-pro.dev</code>). Better Stack hosts the status page on their domain or you can point your status subdomain to it via a CNAME record. Configure the status page to automatically update when monitor status changes — green for operational, yellow for degraded, red for downtime. Add an incident history log so users can see past outages and their resolution times.</p>
<h2>Step 6: Review Monitoring Data and Improve Reliability</h2>
<p>Review your monitoring dashboard regularly. Look for patterns: does your subdomain become slow or unresponsive at certain times of day? Do SSL checks fail after specific events? Use the response time data to identify performance bottlenecks. If your monitoring service provides multi-location checks, compare response times from different geographic regions — high variance may indicate CDN configuration issues. Set up weekly or monthly uptime reports to track trends. Use the insights to improve your infrastructure: add caching, optimize database queries, or upgrade hosting if response times degrade consistently. Higher uptime builds trust with your users and improves SEO.</p>
<h2>Best Practices</h2>
<ul><li>Monitor from multiple geographic locations to catch regional outages</li><li>Set up redundant notification channels (email + Slack + SMS) so you never miss an alert</li><li>Include a health check endpoint in your application that returns database and cache status for deeper monitoring</li><li>Keep your status page URL separate from your main service so it stays accessible during outages</li></ul>
<h2>Conclusion</h2>
<p>Uptime monitoring is essential for any service you care about. Free tools like UptimeRobot and Better Stack provide reliable monitoring, instant alerts, and public status pages for your is-cool-me subdomains. Combined with SSL monitoring and a status page, you can detect and respond to issues before they impact your users significantly.</p>
<h2>FAQ</h2>
<p><strong>How often should I check my subdomain?</strong></p>
<p>For most services, a 5-minute check interval is sufficient. Critical production services may benefit from 1-minute checks, which typically require a paid plan.</p>
<p><strong>Can I monitor non-HTTP services like game servers?</strong></p>
<p>Some monitoring services support TCP and port checks, which work for game servers, SSH, and other non-HTTP services. UptimeRobot's free tier includes TCP monitoring.</p>
<p><strong>What is a good uptime target?</strong></p>
<p>99.9% uptime (about 8.7 hours of downtime per year) is a reasonable target for most personal projects. Commercial services should aim for 99.99% (52 minutes of downtime per year).</p>`,

  'redirect-old-domain-to-new': `<h2>Introduction</h2>
<p>Moving a website from one domain to another is a common task — whether you are migrating from an old personal domain to a free is-cool-me subdomain, rebranding your project, or consolidating multiple domains. A 301 redirect tells browsers and search engines that the content has permanently moved to the new URL, preserving SEO rankings and ensuring visitors reach the right page. This guide covers setting up 301 redirects using Cloudflare Page Rules, Nginx configurations, and Netlify redirect files.</p>
<h2>Prerequisites</h2>
<ul><li>An old domain or subdomain that you want to redirect from</li><li>An is-cool-me subdomain that you want to redirect to (already configured with your site)</li><li>Access to the old domain's DNS settings (or Cloudflare dashboard if on is-cool-me)</li><li>A clear mapping of old URLs to new URLs (if restructuring content)</li></ul>
<h2>Step 1: Choose Your Redirect Method</h2>
<p>There are several ways to implement 301 redirects. Cloudflare Page Rules work if the old domain is on Cloudflare (which is-cool-me domains are). Nginx or Apache configuration works if you control the origin server. Netlify or Vercel redirects work if your site is hosted on those platforms. The simplest method is Cloudflare Page Rules — you can set up a redirect in minutes without touching server configuration. For most is-cool-me subdomain migrations, Cloudflare Page Rules are the recommended approach because they handle redirects at the edge, are free, and do not require server access.</p>
<h2>Step 2: Set Up Redirect with Cloudflare Page Rules</h2>
<p>Log into your Cloudflare dashboard and select the domain you are redirecting from. Navigate to "Rules &rarr; Page Rules". Click "Create Page Rule". Enter the old URL pattern using a wildcard: <code>oldname.is-pro.dev/*</code>. Under "Then the settings", choose "Forwarding URL" and select status code "301 - Permanent Redirect". In the destination URL, enter your new subdomain URL with a placeholder for the captured path: <code>https://newname.is-pro.dev/$1</code>. The <code>$1</code> captures everything matched by the <code>*</code> wildcard, preserving the full URL path. Click "Save and Deploy". The redirect activates immediately on Cloudflare's network. Test it by visiting <code>https://oldname.is-pro.dev/any-page</code> — it should redirect to <code>https://newname.is-pro.dev/any-page</code>.</p>
<h2>Step 3: Preserve SEO During Migration</h2>
<p>To maintain search engine rankings during a domain migration, follow these SEO best practices. First, ensure all old URLs return a 301 (permanent) redirect status, not 302 (temporary). Search engines treat 301s as a signal to transfer ranking signals to the new URL. Second, use the <code>$1</code> wildcard technique to preserve the URL path structure — changing URL structures can dilute SEO value. Third, update your sitemap.xml on the new domain to list all migrated content. Fourth, submit a change of address in Google Search Console: go to "Settings &rarr; Change of address" and enter your new subdomain. This explicitly tells Google that your site has moved. Fifth, notify any external sites that link to your old domain to update their links to the new URL.</p>
<h2>Step 4: Set Up Nginx-Level Redirect (Alternative Method)</h2>
<p>If you control the origin server for the old domain, you can configure redirects in Nginx. In the server block for the old domain, add: <code>server { listen 80; server_name oldname.is-pro.dev; return 301 https://newname.is-pro.dev\$request_uri; }</code>. This captures the full request URI and appends it to the new domain. For HTTPS redirects, repeat with port 443 and include SSL configuration. The <code>\$request_uri</code> Nginx variable includes the path and query string, ensuring complete URL preservation. Test with <code>curl -I https://oldname.is-pro.dev/old-page?param=value</code> and verify the <code>Location</code> header contains the full new URL.</p>
<h2>Step 5: Set Up Netlify Redirects (Hosting Platform Method)</h2>
<p>If your site is hosted on Netlify, create a <code>_redirects</code> file in the publish directory (no file extension). Add rules in the format: <code>https://oldname.is-pro.dev/* https://newname.is-pro.dev/:splat 301!</code>. The <code>!</code> makes this a forced redirect that overrides any conflicting rules. Alternatively, add redirects in <code>netlify.toml</code>: <code>[[redirects]] from = "https://oldname.is-pro.dev/*" to = "https://newname.is-pro.dev/:splat" status = 301 force = true</code>. Deploy the changes. Netlify applies redirects at the edge, so propagation is immediate. For Vercel, use a <code>vercel.json</code> file with redirects configuration: <code>{"redirects": [{"source": "/(.*)", "destination": "https://newname.is-pro.dev/$1", "permanent": true}]}</code>.</p>
<h2>Step 6: Verify the Migration</h2>
<p>After setting up redirects, thoroughly test every aspect of the migration. Visit multiple old URLs and confirm they redirect to the correct new URLs with a 301 status code. Use the HTTP Headers Checker tool on is-cool-me to verify the redirect chain. Check that query parameters are preserved in the redirect. Test that all site features work on the new domain — forms, login, API calls, and embedded content. Update any hardcoded URLs in your application code, database, or email templates. Monitor Google Search Console for crawl errors on the old domain. Set a reminder to check back in 30 days to confirm search rankings have transferred and the old domain is no longer being indexed.</p>
<h2>Best Practices</h2>
<ul><li>Keep the old domain active and redirecting for at least 6-12 months after migration</li><li>Do not redirect all URLs to the homepage — preserve the URL structure to maintain SEO value for individual pages</li><li>Update internal links in your content to point to the new domain directly rather than relying on redirects</li><li>Monitor the old domain's redirect logs for unexpected traffic patterns that might indicate broken redirect rules</li></ul>
<h2>Conclusion</h2>
<p>Redirecting an old domain to your new is-cool-me subdomain is essential for preserving traffic, SEO rankings, and user experience during a migration. Cloudflare Page Rules offer the simplest implementation, while Nginx and Netlify configurations provide alternatives for server-level control. Whichever method you choose, proper 301 redirects with URL preservation ensure a smooth transition for both users and search engines.</p>
<h2>FAQ</h2>
<p><strong>How long should I keep the old domain active?</strong></p>
<p>Keep the redirect active for at least 6 months to a year. Search engines need time to transfer ranking signals, and users may have bookmarks pointing to the old URL.</p>
<p><strong>Can I redirect a subdomain that is not on Cloudflare?</strong></p>
<p>Yes, you can redirect from any domain using server configuration, HTML meta refresh, or JavaScript redirect. However, server-level 301 redirects are the most SEO-friendly method.</p>
<p><strong>Will a 301 redirect affect my search engine rankings?</strong></p>
<p>When implemented correctly with 301 status codes and matching URL paths, most SEO value transfers to the new domain. Rankings may fluctuate temporarily but should recover within 1-3 months.</p>`
};
